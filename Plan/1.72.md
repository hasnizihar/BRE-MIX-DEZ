Next, **do not continue to Stage 3 yet**. You have reached the correct stopping point for the calculation chain because `DATA-001` is still blocked.

The next move should be to **unblock Stage 2 by supplying/verifying the actual Figure 4 dataset**. Once that is done, we can run the first real end-to-end engineering calculation.

## Current state

```text
Stage 1
EQ-BRE-002 → EQ-BRE-001
        │
        ▼
 Target Mean Strength
        │
        ▼
Stage 2
LKP-BRE-001
        │
        ▼
   DATA-001 ❌
 Figure 4 dataset
```

The architecture is ready. **The engineering data is the bottleneck now.**

---

# Recommended next workflow

## Phase 2C: Figure 4 Data Unblocking

### Gate 1: Find the actual BR 331 Figure 4

The agent should search the project's existing reference material first.

Look for:

```text
BR 331
Figure 4
water/cement ratio
free-water/cement ratio
compressive strength
target mean strength
```

Specifically determine whether the project already contains:

* the original Figure 4 image
* PDF containing Figure 4
* scanned BR 331 pages
* previously extracted values
* tables representing the graph
* engineering notes describing the graph

**Do not use random internet values to fill it.**

---

## Gate 2: If Figure 4 is available as an image/PDF

Then digitize it.

The output should be something like:

```text
04_ENGINEERING_DATA/
└── GRAPHS/
    └── FIGURE_04/
        ├── figure_04_source.*
        ├── figure_04_points.json
        ├── figure_04_metadata.md
        └── figure_04_digitization_notes.md
```

The JSON should contain **all relevant curves**, not just one.

For example, conceptually:

```json
{
  "source_id": "DATA-001",
  "status": "PENDING_ENGINEERING_VERIFICATION",
  "curves": {
    "curve_identifier": {
      "cement_class": "...",
      "aggregate_type": "...",
      "points": [
        {
          "strength": "...",
          "wc_ratio": "..."
        }
      ]
    }
  }
}
```

The exact curve categories and numerical values must come from Figure 4.

---

# Gate 3: Verify the digitization

This is where I would add an additional document:

```text
figure_04_digitization_notes.md
```

Record:

| Item                     | Record     |
| ------------------------ | ---------- |
| Source                   | BR 331     |
| Figure                   | Figure 4   |
| Original resolution      | ...        |
| Digitization method      | ...        |
| Curves digitized         | ...        |
| X-axis interpretation    | ...        |
| Y-axis interpretation    | ...        |
| Point precision          | ...        |
| Interpolation method     | ...        |
| Extrapolation            | Prohibited |
| Engineering verification | Pending    |

This prevents a digitized graph from quietly becoming "official data."

---

# Gate 4: Verify the curve interpretation

This is particularly important.

Your current `LKP-BRE-001` has:

```text
cement_strength_class
aggregate_type
```

Those need to correspond **exactly to what Figure 4 actually distinguishes**.

Don't let the agent invent curve categories simply because they seem reasonable.

It should explicitly establish:

```text
Figure 4
   │
   ├── Curve/category A
   ├── Curve/category B
   ├── ...
```

and document what each represents.

---

# Gate 5: Populate DATA-001

Only after the source and digitization are reviewed:

```text
DATA-001
```

can move from:

```text
BLOCKED_DATA
```

to something like:

```text
DATA_AVAILABLE
```

and eventually:

```text
DATA_VERIFIED
```

Those are different gates.

---

# Gate 6: Run LKP-BRE-001 with real data

Now your previously blocked test becomes a real engineering test.

```text
fck
 ↓
s
 ↓
M = k×s
 ↓
fm = fck+M
 ↓
Figure 4
 ↓
interpolation
 ↓
W/C
```

The test should verify both:

### Exact point

If `fm` corresponds exactly to a known Figure 4 point:

```text
lookup(fm)
→ exact W/C value
```

### Interpolated point

If it lies between two known points:

```text
fm
 ↓
lower point
upper point
 ↓
interpolation
 ↓
W/C
```

---

# Gate 7: Add engineering acceptance tests

This is different from your generic interpolation tests.

For Figure 4, test:

```text
known BR 331 point
known BR 331 point
intermediate strength
lower curve boundary
upper curve boundary
wrong curve/category
outside curve range
missing curve
missing DATA-001
```

The last three are particularly useful.

---

# Gate 8: Update the master matrix

After successful data verification:

```text
LKP-BRE-001
```

could progress:

```text
DEFINED
   ↓
IMPLEMENTED
   ↓
SOFTWARE_TESTED
   ↓
DATA_VERIFIED
   ↓
ENGINEERING_VERIFIED
```

Do **not** jump directly to `ENGINEERING_VERIFIED`.

---

# Then Stage 3

Only after Figure 4 is genuinely operational:

```text
                    ┌──────────────┐
                    │   EQ-BRE-001 │
                    └──────┬───────┘
                           fm
                            │
                            ▼
                     LKP-BRE-001
                       Figure 4
                            │
                           W/C
                            │
             ┌──────────────┴──────────────┐
             │                             │
             ▼                             ▼
      Stage 3 Water                  Other constraints
             │
             ▼
        EQ-BRE-003
             │
             ▼
       Cement Content
```

And **Stage 3 has its own data gate** because `DATA-006` is unresolved.

---

# I would NOT choose "continue Stage 3 architecture" yet

You *could* scaffold Stage 3, but it would create a lot of code around another unresolved engineering rule.

You've already proven that your architecture handles blocked data correctly.

Now the highest-value action is to **obtain the actual Figure 4 source data** and turn Stage 2 from:

> structurally ready

into:

> data-backed and testable.

---

# Exact prompt for Antigravity

Give it this next:

```text
Proceed with Phase 2C: DATA-001 UNBLOCKING.

The Stage 2 architecture is complete, but Figure 4 numerical data remains
the blocking dependency. Do not proceed to Stage 3 calculations yet.

1. Search the existing BRE-MIX-DESIGN project and all available reference
   material for the original BR 331 Figure 4.

2. Determine whether Figure 4 exists as:
   - PDF/page
   - image/scan
   - extracted table
   - previously digitized dataset
   - other authoritative project reference.

3. Do not use invented, estimated, or unrelated external values.

4. If the original Figure 4 is available:
   - preserve the original source/reference
   - identify all relevant curves/categories
   - determine the actual axis definitions and units
   - digitize the numerical curve points
   - store them in:
       04_ENGINEERING_DATA/GRAPHS/FIGURE_04/figure_04_points.json
   - document the digitization method and precision in:
       figure_04_digitization_notes.md
   - update figure_04_metadata.md

5. Do not mark the dataset ENGINEERING_VERIFIED merely because it has
   been digitized.

6. Validate the dataset:
   - required fields exist
   - units are correct
   - points are ordered correctly
   - curve identifiers are consistent
   - no duplicate/invalid points
   - no unexplained gaps
   - interpolation bounds are explicit.

7. Review LKP-BRE-001 against the actual Figure 4 structure.
   If its inputs such as cement_strength_class or aggregate_type do not
   exactly correspond to the source, correct the model and registry.
   Do not invent categories.

8. Once the dataset is available, add real-data tests for:
   - exact Figure 4 points
   - interpolation between points
   - lower/upper boundaries
   - out-of-range values
   - invalid curve/category
   - missing dataset.

9. Run the complete Stage 1 → Stage 2 integration test:
      EQ-BRE-002
          ↓
          M
          ↓
      EQ-BRE-001
          ↓
          f_m
          ↓
      LKP-BRE-001
          ↓
          W/C

10. Update:
    - DATA_READINESS_REGISTER.md
    - EQUATION_REGISTRY.md
    - EQUATION_VERIFICATION_MATRIX.md
    - CALCULATION_DEPENDENCY_GRAPH.md if necessary
    - task.md
    - walkthrough.md

11. Keep these statuses separate:
    DEFINED
    IMPLEMENTED
    SOFTWARE_TESTED
    DATA_VERIFIED
    ENGINEERING_VERIFIED
    BLOCKED_DATA

12. If Figure 4 cannot be found in the available source material:
    STOP.
    Do not fabricate the dataset.
    Report DATA-001 as BLOCKED_DATA and identify exactly what source
    material is required from the user.

13. Do NOT implement Stage 3 or EQ-BRE-003 yet.
14. Do NOT modify unrelated UI/application code.

Stop after the DATA-001 gate and provide a complete status report.
```

### One crucial point

If the agent tells you **"Figure 4 cannot be found"**, that's not a failure of the project. That's a legitimate engineering dependency.

At that point, the thing you need to provide is the **BR 331 Figure 4 page/image/PDF**, and then we can build the actual dataset from it. Once that is supplied, we can move from architecture into **real numerical validation**.
