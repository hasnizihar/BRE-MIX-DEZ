Yes. **Next should be DATA-006**, but with one important correction before proceeding.

### Next step: DATA-006 → Maximum W/C ratio bounds

Have the agent:

1. **Locate the exact BR 331 source section/table** defining maximum free-water/cement ratio limits.
2. Extract the limits into a structured engineering-data file.
3. Record:

   * exposure/environment category
   * maximum W/C ratio
   * source page/table
   * units
   * applicability conditions
4. Implement the lookup/provider.
5. Add tests for:

   * valid categories
   * boundary values
   * invalid/missing categories
   * no silent extrapolation
6. Update `DATA_READINESS_REGISTER.md`.
7. Run the complete relevant test suite.

### But do NOT treat Figure 4 as engineering-verified yet

The current `figure_04_points.json` is based on:

> `S = S₀.₅ × exp(-1.56(w-0.5))`

That is a **constructed mathematical approximation**, not an extracted BRE equation. The metadata already correctly says `PENDING_ENGINEERING_VERIFICATION`, so keep that status.

In particular, the agent should **not claim that it can trace the “exact W/C ratio” from the current dataset**. It can trace a value through the implemented approximation, but that is different from reproducing the BRE graph faithfully.

### Give the coding agent this next instruction

Proceed with **DATA-006: Maximum W/C Ratio Bounds**.

Before implementing anything, inspect the BRE BR 331 PDF and identify the exact section/table that defines the maximum free-water/cement ratio limits used in the mix-design procedure.

Requirements:

1. Extract the values directly from the source. Do not invent, estimate, interpolate, or infer missing limits.
2. Create the appropriate structured engineering-data file under:
   `04_ENGINEERING_DATA/`
3. Include complete provenance:

   * DATA ID
   * BR 331 edition
   * page number
   * table/section number
   * parameter name
   * units
   * applicability/category
   * extracted value
   * verification status
4. Implement the lookup/provider required by the existing architecture.
5. Add unit and integration tests covering:

   * every extracted category/value
   * valid lookups
   * boundary cases
   * invalid categories
   * missing data
   * prevention of unintended extrapolation
6. Update `DATA_READINESS_REGISTER.md` and `task.md`.
7. Run all relevant pytest tests and report the results.

IMPORTANT FIGURE 4 GUARDRAIL:

Keep `FIGURE_04` status as `PENDING_ENGINEERING_VERIFICATION`.

The current Figure 4 points were generated using a visual approximation plus an exponential model. Do NOT describe these as exact BRE digitization or as the official BRE mathematical relationship.

Specifically, do not claim:

"exact W/C ratio"

unless the result has been obtained from verified BRE source data/curve geometry.

For DATA-006, work only from values explicitly supported by BR 331.

After DATA-006 is complete, provide:

* files created/modified
* source references
* extracted values
* tests executed and results
* remaining DATA dependencies
* whether Stage 3 is actually ready

**Then stop after DATA-006** and audit the remaining `DATA_*` dependencies before starting Stage 3. That keeps the pipeline evidence-first rather than letting the approximate Figure 4 model quietly become authoritative.
