Yes. At this point, **do not move into coding/refactoring yet**. The browser subagent is still extracting Figure 3, so the next phase should be an evidence-first verification workflow.

## BRE Mix Design: Detailed Workflow After Figure 3 Extraction

### Phase 1: Finish Figure 3 source verification

**Goal:** Establish DATA-004 as a trustworthy engineering-data source.

Wait for the browser/subagent result, then have the agent produce:

1. **Exact source identification**

   * Figure number
   * Exact title
   * Printed page
   * PDF page
   * Section(s) referencing it

2. **Graph structure**

   * X-axis
   * Y-axis
   * Line A meaning
   * Line B meaning
   * Any strength ranges/breakpoints
   * Units
   * Any notes surrounding the figure

3. **Complete A/B values**

   Do **not** accept only:

   > Line A = 8, Line B = 4 for fc ≥ 20 MPa

   Instead determine the **entire piecewise relationship** represented by Figure 3.

   For example, the investigation should establish something structurally like:

   ```text
   fc range       Line A       Line B
   --------       -------      -------
   ...
   ...
   ...
   ```

   Only populate values actually supported by the source.

4. **Extraction method**

   * Was the value explicitly printed?
   * Was it read from graph coordinates?
   * Was pixel calibration used?
   * What uncertainty exists?
   * Can the extracted points be independently reproduced?

### Phase 2: Separate Figure 3 data from the statistical procedure

This is important.

Create two separate concepts:

```text
DATA-004
Figure 3 lookup data
       ↓
standard deviation guidance

PROCEDURE
Which line applies?
       ↓
n < 20 → Line A
n ≥ 20 → calculated s, subject to Line B minimum
```

Do **not** encode the entire thing as one giant lookup table.

The engine should eventually behave conceptually like:

```text
Input:
    characteristic strength fc
    number of previous results n
    calculated standard deviation s, if applicable

             ↓

Figure 3
    Line A / Line B values

             ↓

selection rule
    n < 20
        → use Line A

    n ≥ 20
        → use calculated s
        → enforce Line B minimum

             ↓

design standard deviation
             ↓
margin M = k × s
             ↓
target mean strength fm = fc + M
```

But **don't implement this yet**.

---

# Phase 3: Investigate the standard deviation calculation

This is the next major question after Figure 3.

Search the BR 331 source for:

* `standard deviation`
* `s`
* `20 results`
* `test results`
* `previous results`
* `statistical`
* `calculated`
* `results available`

Determine exactly what BR 331 says when:

```text
n ≥ 20
```

The critical question is:

> **Does BR 331 itself define how s is calculated, or does it assume s is already available from previous test results?**

If BR 331 does **not** provide the statistical calculation formula, do not invent one.

Instead classify:

```text
calculated_s = EXTERNAL_INPUT
```

or whatever project terminology is ultimately selected.

That distinction matters because otherwise the software could appear to implement BRE while actually inserting a statistical procedure from another standard.

---

# Phase 4: Investigate the defective proportion and k

Next audit the relationship:

```text
Margin = k × s
```

Determine from BR 331:

### Inputs

* characteristic strength `fc`
* proportion defective
* factor `k`
* standard deviation `s`

### Questions to answer

1. Where does BR 331 define `k`?
2. Is `k` explicitly tabulated?
3. Is it derived from statistical theory?
4. Does BR 331 give only examples such as:

   * 5% defective → 1.64
   * 10% defective → 1.28
5. Are other values explicitly supported?

Keep the provenance separate:

```text
BRE lookup/data
    ↓
k

External user input
    ↓
proportion defective

External/user statistical data
    ↓
calculated s
```

Do not build an arbitrary k lookup table from memory.

---

# Phase 5: Audit EQ-BRE-001 before changing it

Only after DATA-004 is verified should the agent inspect the current implementation.

Current problem:

```text
fm = fc + m
```

with `m` effectively supplied directly.

That is incomplete because the BRE procedure is:

```text
s
↓
M = k × s
↓
fm = fc + M
```

The audit should identify exactly:

* current function signature
* current inputs
* current outputs
* current rounding
* current tests
* current provenance
* which parts are missing

### Important

Do **not** immediately rewrite the module.

First produce a change specification such as:

```text
EQ-BRE-001 required inputs:
    fc
    s
    k

EQ-BRE-001 calculations:
    M = k × s
    fm = fc + M

Rounding:
    source-supported rule only

Figure 3 dependency:
    DATA-004

External inputs:
    ...
```

Then review that against BR 331.

---

# Phase 6: Correct the rounding rule

The current implementation apparently does:

```python
round(fm, 1)
```

That needs to be challenged.

The source example gives:

```text
fc = 30
s = 8
k = 1.64

M = 1.64 × 8
  = 13.12

fm = 30 + 13.12
   = 43.12
```

BR 331 presents the margin/target in its design example with the appropriate source presentation precision.

The agent needs to establish **exactly what BR 331 requires**, rather than assuming Python decimal rounding.

So separate:

```text
engineering calculation
        ↓
full precision

presentation
        ↓
BRE-required precision
```

Do not contaminate the underlying calculation with display rounding.

---

# Phase 7: Build DATA-004 files

Once the extraction is verified, the agent should create/update:

```text
04_ENGINEERING_DATA/
└── GRAPHS/
    └── FIGURE_03/
        ├── figure_03_metadata.md
        ├── figure_03_digitization_notes.md
        └── figure_03_points.json
```

### `figure_03_metadata.md`

Should contain:

```text
Source
Exact title
BR 331 edition
Printed page
PDF page
Section references
Axes
Units
Line A definition
Line B definition
Strength ranges
Verified values
Extraction method
Provenance
Status
```

### `figure_03_digitization_notes.md`

Document:

```text
Source image
Resolution
Calibration method
Reference points
Pixel → engineering coordinate mapping
Extracted points
Uncertainty
Manual interpretation
Verification procedure
```

### `figure_03_points.json`

Keep the raw engineering data structured.

Something conceptually like:

```json
{
  "figure": "Figure 3",
  "status": "PENDING_ENGINEERING_VERIFICATION",
  "source": {
    "document": "...",
    "page": "...",
    "printed_page": "..."
  },
  "line_a": [],
  "line_b": []
}
```

The exact schema should follow what the agent discovers, not be invented beforehand.

---

# Phase 8: Add provenance guards

The project already has an important pattern from Figures 4 and 5.

Apply the same architecture:

```text
Verified engineering data
        ↓
Provider
        ↓
Calculation
```

and:

```text
Synthetic / unverified data
        ↓
BLOCK
        ↓
EngineeringDataUnavailableError
```

Therefore:

### Production

```text
DATA-004 verified
→ provider allowed
```

### Not verified

```text
DATA-004 unverified
→ production calculation blocked
```

Tests may use controlled synthetic fixtures, but they must be clearly isolated from production engineering data.

---

# Phase 9: Refactor EQ-BRE-001

Only after Phase 1 through Phase 8 are complete.

The implementation should then represent the actual chain:

```text
fc
│
├── Figure 3
│     ├── Line A
│     └── Line B
│
├── n
│
├── s, if n ≥ 20
│
└── k
      │
      ▼
  selected s
      │
      ▼
  M = k × s
      │
      ▼
  fm = fc + M
```

And importantly:

### Do not bury Figure 3 logic inside `eq_bre_001.py`

Prefer:

```text
engineering data
       ↓
figure_03 provider
       ↓
standard deviation selector
       ↓
EQ-BRE-001
```

That keeps the source graph independently auditable.

---

# Phase 10: Tests

After implementation, add tests in layers.

### A. Figure 3 data tests

Verify:

* schema
* source metadata
* units
* expected ranges
* Line A
* Line B
* no synthetic production data

### B. Standard deviation selection tests

At minimum:

```text
n < 20
→ Line A

n ≥ 20
→ calculated s

calculated s < Line B
→ Line B governs

calculated s > Line B
→ calculated s governs
```

But only if these rules are actually confirmed by BR 331.

### C. EQ-BRE-001 tests

Test:

```text
M = k × s
fm = fc + M
```

with source-supported examples.

### D. Boundary tests

Examples:

```text
n = 19
n = 20

s exactly equal to Line B
s below Line B
s above Line B
```

### E. Invalid-input tests

For example:

```text
fc <= 0
k <= 0
s < 0
n < 0
missing s when n >= 20
invalid defective proportion
```

Only enforce constraints that are justified by the engineering procedure or general mathematical validity.

---

# Phase 11: Run the complete regression suite

After refactoring:

```text
Figure 3 tests
+
EQ-BRE-001 tests
+
Stages 2–5
+
all existing tests
```

The key distinction remains:

> **58+ tests passing proves software behavior, not that Figure 3 has been correctly digitized.**

The final verification report should explicitly separate:

| Layer                   | Status             |
| ----------------------- | ------------------ |
| Source location         | Verified           |
| Figure interpretation   | Verified           |
| Digitized values        | Verified / Pending |
| Statistical procedure   | Verified           |
| Software implementation | Tested             |
| Engineering validation  | Pending / Verified |

---

# Phase 12: Update the project readiness register

Update:

```text
DATA_READINESS_REGISTER
```

with something like:

```text
DATA-004 | Figure 3 | ...
Status: ...
Source: BR 331 ...
Production usable: YES/NO
Verification evidence: ...
```

Then update:

```text
TASK / TODO
walkthrough.md
provenance documentation
```

so there is a clean audit trail.

---

# Phase 13: Freeze Stage 1

Do **not** declare Stage 1 complete merely because EQ-BRE-001 tests pass.

Stage 1 is complete only when:

```text
DATA-004
    ↓
verified

standard deviation procedure
    ↓
verified

EQ-BRE-001
    ↓
implemented

tests
    ↓
passing

provenance
    ↓
documented

production guards
    ↓
working
```

---

# Phase 14: Then return to the unresolved engineering-data chain

After Figure 3, your unresolved engineering data should still be:

```text
DATA-001  Figure 4   → PENDING_ENGINEERING_VERIFICATION
DATA-002  Figure 5   → SYNTHETIC_PLACEHOLDER
DATA-003  Figure 6   → PENDING_USER_DATA / blocked
DATA-004  Figure 3   → current investigation
```

So the larger roadmap becomes:

```text
                 BR 331
                    │
       ┌────────────┴────────────┐
       ↓                         ↓
   FIGURE 3                  FIGURE 4
  DATA-004                  DATA-001
       │                         │
       ↓                         ↓
 Standard deviation          W/C ratio
       │                         │
       └──────────┬──────────────┘
                  ↓
             Cement C3
                  ↓
             FIGURE 5
             DATA-002
                  ↓
            Wet density
                  ↓
             Aggregate C4
                  ↓
             FIGURE 6
             DATA-003
                  ↓
          Fine aggregate C5
                  ↓
          Coarse aggregate
                  ↓
             Final mix
```

## What to send the agent **right now**

Since the browser subagent is still working, send this next instruction. It tells the agent to finish the investigation rather than jumping prematurely into implementation.

Continue the DATA-004 / Figure 3 investigation from the current browser extraction. Do NOT refactor EQ-BRE-001 yet.

Wait for and inspect the browser/subagent result, then complete the following in order:

1. Identify the exact Figure 3 title, BR 331 edition, printed page, PDF page, and all relevant Section references.

2. Extract and document the complete Figure 3 structure:

   * X-axis
   * Y-axis
   * units
   * Line A definition
   * Line B definition
   * all strength ranges/breakpoints
   * any notes or exceptions shown around the figure.

3. Verify whether the previously reported values of Line A = 8 N/mm² and Line B = 4 N/mm² for fc >= 20 N/mm² are actually supported by the source graphic/text. Do not accept these values merely because they were reported previously.

4. Determine the COMPLETE piecewise Line A and Line B relationships across the figure, not just the >=20 N/mm² region. If values cannot be reliably extracted, explicitly leave them unverified rather than estimating or creating synthetic points.

5. Clearly separate:
   A. source-derived Figure 3 data,
   B. procedural rules for selecting standard deviation,
   C. external/statistical inputs,
   D. calculated quantities.

6. Investigate the rule for the number of previous test results:

   * n < 20
   * n >= 20
     Determine exactly what BR 331 says about Line A, Line B, and calculated standard deviation s.

7. Determine whether BR 331 itself defines the statistical formula for calculating s when n >= 20. If it does not, classify calculated s as an external/statistical input rather than inventing a formula.

8. Investigate the margin procedure:
   M = k × s
   fm = fc + M
   Determine where k and the proportion defective are defined, including all source-supported values/examples. Do not invent a k lookup table.

9. Investigate the source-supported rounding/presentation precision for M and fm. Do not assume Python round() or one-decimal rounding.

10. Create/update:
    04_ENGINEERING_DATA/GRAPHS/FIGURE_03/figure_03_metadata.md
    04_ENGINEERING_DATA/GRAPHS/FIGURE_03/figure_03_digitization_notes.md
    04_ENGINEERING_DATA/GRAPHS/FIGURE_03/figure_03_points.json

11. Use calibrated extraction from the actual Figure 3 image where graph digitization is required. Do NOT create synthetic curves or physically estimated values.

12. If reliable extraction is not possible, keep the relevant data unverified and use the project's established provenance status such as PENDING_ENGINEERING_VERIFICATION. Do not promote approximate values into production engineering data.

13. Add/update provider/schema tests and production-blocking tests. Production must raise EngineeringDataUnavailableError whenever Figure 3 data required by the calculation is not verified.

14. Do NOT modify EQ-BRE-001 implementation yet. Do not change its rounding, inputs, or calculation chain until this DATA-004 investigation is independently complete.

15. Update DATA_READINESS_REGISTER, provenance documentation, task/walkthrough documentation, and any verification matrix affected by this investigation.

Final report MUST contain:

* exact Figure 3 title
* source location
* axes and units
* complete Line A relationship
* complete Line B relationship
* verified values and their provenance
* n < 20 rule
* n >= 20 rule
* whether calculation of s is defined by BR 331
* k/proportion-defective provenance
* rounding rule
* digitization method and feasibility
* DATA-004 status
* files created/updated
* tests run and results
* whether production is blocked
* recommended next step

Engineering guardrail remains absolute: no invented BRE values, no synthetic production data, and no silent substitution of assumptions for source evidence.

Once that report comes back, **paste it here before letting the agent refactor EQ-BRE-001**. That report is the decision gate for the next implementation step.
