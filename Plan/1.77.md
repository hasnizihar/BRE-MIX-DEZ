Yes, **proceed to EQ-BRE-006 investigation**, but with one important correction: don't assume the ID `EQ-BRE-006` until the source/code registry confirms it. The agent should first verify that the coarse aggregate calculation is actually the next distinct BRE calculation and identify its exact source location.

Also, the C5 result looks properly separated: **27% is an input from Figure 6, while C5 itself only performs the arithmetic.** The 580.5 → 580 distinction should remain documented as BRE's presentation/rounding, not hidden inside the core equation unless BR 331 explicitly specifies that rounding rule.

Use this:

Proceed to investigate the **Coarse Aggregate Content** calculation after EQ-BRE-005.

### Important: verify the equation ID first

Do NOT assume the next equation is `EQ-BRE-006`.

First perform a source-first investigation of BR 331 and determine:

* Exact section/subsection
* Printed page
* PDF page
* Calculation identifier, if BR 331 gives one
* Exact wording
* Exact mathematical relationship
* Whether this is a distinct BRE calculation or simply an arithmetic step following C5

Only then assign the project's equation ID.

### 1. Source-first investigation

Locate the exact BR 331 procedure for determining coarse aggregate content.

The suspected relationship is:

`Coarse aggregate content = Total aggregate content - Fine aggregate content`

Verify this directly against BR 331.

Do not rely on the previous agent's statement alone.

Determine:

* Total aggregate input and units
* Fine aggregate input and units
* Coarse aggregate output and units
* Any rounding requirements
* Any constraints or validity checks
* Any worked examples that demonstrate the calculation
* Where the resulting coarse aggregate value appears in the worked example/table

### 2. Rounding must be investigated separately

The previous C5 report identified:

`2150 × 27% = 580.5`

while the BR 331 example presents approximately `580`.

Investigate whether BR 331 specifies:

* rounding to the nearest kg/m³,
* truncation,
* presentation rounding,
* or simply displays a rounded worked-example value.

Do NOT invent a universal rounding rule.

If rounding is merely presentation-level, keep the equation's internal result unrounded and document that distinction.

### 3. Engineering specification

Create the implementation plan/specification before coding.

Define:

**Inputs**

* Total aggregate content, kg/m³
* Fine aggregate content, kg/m³

**Output**

* Coarse aggregate content, kg/m³

Define the expected mathematical and unit behaviour based only on BR 331 evidence.

### 4. Provenance architecture

Keep the equation independent from the graph data.

The intended dependency should be:

`DATA-003 Figure 6`
→ `fine aggregate proportion`
→ `EQ-BRE-005`
→ `fine aggregate content`
→ `coarse aggregate calculation`

The coarse aggregate equation should receive the upstream values as arguments.

It should NOT directly access:

`figure_06_points.json`

### 5. Constraints and validation

Determine appropriate engineering constraints from the source.

At minimum investigate:

* negative total aggregate
* negative fine aggregate
* fine aggregate greater than total aggregate
* zero values
* resulting negative coarse aggregate

Do not automatically invent constraints simply because they seem physically reasonable. Where a validation rule is not explicitly from BR 331, distinguish it as a **software/domain safety validation** rather than a BRE rule.

### 6. Worked-example verification

Use actual BR 331 numerical examples where available.

For each example:

* quote/reference the source location
* identify input values
* calculate the result
* compare with the BR 331 presented result
* explain any difference caused by rounding

Do not fabricate a worked example if BR 331 does not provide one.

### 7. Tests

Create the appropriate equation test file after confirming the equation ID.

Test:

* normal valid calculation
* exact arithmetic
* BR 331 worked example(s)
* boundary conditions
* invalid inputs
* fine aggregate > total aggregate
* resulting coarse aggregate ≤ 0
* unit consistency
* rounding behaviour, if explicitly required by BR 331

Also add an integration test for the Stage 5/aggregate pipeline where appropriate.

The integration path must preserve the existing DATA-003 lock.

If Figure 6 is unavailable, the production pipeline must stop before producing fine/coarse aggregate values.

### 8. Regression testing

Run:

1. New equation tests
2. Relevant Stage 5 tests
3. Previous Stage 1-5 tests
4. Full pytest suite

Report the exact test count.

### 9. Documentation

Update:

* `EQUATION_VERIFICATION_MATRIX.md`
* `DATA_READINESS_REGISTER.md` if required
* `task.md`
* `walkthrough.md`
* provenance/index documentation

Keep:

`DATA-003 = PENDING_USER_DATA`

and:

`EQ-BRE-005 = IMPLEMENTED_PENDING_VERIFICATION`

Do not mark the overall Stage 5 as engineering-verified merely because the arithmetic tests pass.

### Final report

Return:

1. Exact source location
2. Confirmed equation/calculation ID
3. Exact formula
4. Inputs/outputs/units
5. Rounding rule and evidence
6. Worked-example comparison
7. Implementation files
8. Test files
9. Test results
10. DATA-003 dependency
11. Production safety status
12. Updated verification status
13. Next dependency

**Critical rule:** verify the source before deciding that this is `EQ-BRE-006`. Do not let the equation numbering become an assumption.

### Why this is the right next move

You now have:

**C4**
`D - C - W` → total aggregate

**C5**
`Total aggregate × fine proportion` → fine aggregate

Then most likely:

**C6 / next verified calculation**
`Total aggregate - fine aggregate` → coarse aggregate

So we're basically assembling the aggregate side of the mix one brick at a time. The remaining big engineering-data bottleneck is still **Figure 6**, not these arithmetic equations.

Once the agent returns the source investigation for the coarse aggregate step, send it here. We should verify the source and rounding before letting it lock in the next equation.
