Yes. **The next gate is DATA-001 / Figure 4.** The EQ-BRE-001 block is now in a good state: source data for Figure 3 is verified, the calculation has been separated from presentation rounding, and the regression suite is passing.

The important thing now is to **not repeat the earlier Figure 4 synthetic-data mistake**. Treat Figure 4 as a fresh engineering-source investigation.

# DATA-001 / Figure 4: Detailed workflow

## 0. Freeze the current checkpoint

Before starting Figure 4, establish this baseline:

```text
DATA-004 Figure 3        VERIFIED
EQ-BRE-001               IMPLEMENTED_PENDING_VERIFICATION
Tests                    65 PASSING

DATA-001 Figure 4        PENDING_ENGINEERING_VERIFICATION
DATA-002 Figure 5        SYNTHETIC_PLACEHOLDER
DATA-003 Figure 6        PENDING_USER_DATA
```

Do **not** change:

* Figure 3 data
* standard deviation implementation
* EQ-BRE-001
* Figure 5
* Figure 6

unless the Figure 4 work exposes a genuine dependency defect.

---

# 1. Locate and inspect Figure 4

First, have the agent find the **actual Figure 4 in the BR 331 PDF**.

It needs to record:

| Item                      | Required |
| ------------------------- | -------- |
| Exact figure title        | Yes      |
| Printed page              | Yes      |
| PDF page                  | Yes      |
| Section references        | Yes      |
| X-axis                    | Yes      |
| Y-axis                    | Yes      |
| Units                     | Yes      |
| Curves                    | Yes      |
| Curve parameters          | Yes      |
| Notes around graph        | Yes      |
| Applicable strength range | Yes      |
| Applicable W/C range      | Yes      |

Do not start digitizing until the graph structure is understood.

---

# 2. Understand what Figure 4 actually represents

Figure 4 is the critical lookup in:

```text
target mean strength
        ↓
Figure 4
        ↓
free-water/cement ratio
```

But don't assume the graph is simply:

```text
strength → W/C
```

Determine exactly:

* Is the strength axis characteristic strength or target mean strength?
* Is the concrete strength based on 28-day cube strength?
* What aggregate condition/type applies?
* Are there multiple curves?
* Are curves differentiated by cement type?
* Are there age/curing conditions?
* Does the graph distinguish crushed/uncrushed aggregate?
* What interpolation is expected?
* Are there limiting W/C values elsewhere in the procedure?

This matters because Figure 4 could be deceptively simple while having important qualifiers in the surrounding text.

---

# 3. Audit the surrounding Section 5.1 text

Do not treat the graph independently.

Read the complete section dealing with:

> Selection of target free-water/cement ratio

Determine the exact sequence:

```text
fm
 ↓
Figure 4
 ↓
calculated free-W/C
 ↓
maximum specified free-W/C
 ↓
final free-W/C
```

You already established the maximum W/C rule earlier:

$$
(W/C)_{final}
=
\min[(W/C)_{strength},(W/C)_{max}]
$$

where the maximum specified W/C is an **external input**.

Confirm that Figure 4 really supplies the first term.

---

# 4. Identify every curve in Figure 4

This is the most important part.

The agent should create a table like:

```text
Curve | Meaning | Source evidence
------|---------|---------------
A     | ...     | ...
B     | ...     | ...
C     | ...     | ...
```

Do not encode a curve simply because it visually appears to exist.

For every curve, identify:

```text
curve parameter
engineering meaning
units
source description
```

If the graph contains only one relationship, explicitly document that too.

---

# 5. Determine the graph's usable domain

Find the actual limits.

For example, establish:

```text
Strength:
minimum = ?
maximum = ?

W/C:
minimum = ?
maximum = ?
```

Also determine whether the graph is intended to be interpolated outside its visible domain.

**Do not extrapolate.**

Production behavior should be:

```text
input inside verified graph domain
        ↓
interpolate

input outside verified domain
        ↓
EngineeringDataUnavailableError
```

unless BR 331 explicitly provides an extrapolation rule.

---

# 6. Digitize Figure 4 properly

This is where the previous synthetic-data problem must not return.

Use the actual PDF figure.

Preferred process:

```text
PDF
 ↓
high-resolution render
 ↓
crop Figure 4
 ↓
identify graph axes
 ↓
calibrate pixel coordinates
 ↓
identify curve coordinates
 ↓
convert pixels → engineering values
 ↓
store raw extracted points
```

Do **not** do:

```text
"I know concrete behavior approximately"
             ↓
fit exponential curve
             ↓
call it BRE Figure 4
```

That would recreate the DATA-001 provenance problem.

---

# 7. Use anchor-point calibration

The digitization notes should explicitly record the calibration.

For example:

```text
Pixel coordinate     Engineering coordinate
─────────────────    ─────────────────────
(x1, y1)             (W/C₁, strength₁)
(x2, y2)             (W/C₂, strength₂)
...
```

Document:

* graph origin
* axis endpoints
* tick positions
* scale
* pixel-to-value mapping
* curve extraction method
* interpolation method
* extraction uncertainty

If the graph is linear in either axis, verify that before implementing interpolation.

---

# 8. Do not immediately convert the graph into an equation

This is important.

If Figure 4 is graphically defined, your **source of truth should remain the digitized graph data**.

Prefer:

```text
figure_04_points.json
        ↓
Figure4Provider
        ↓
validated interpolation
```

rather than:

```python
wc = 1.234 - 0.0123 * strength
```

unless the source itself explicitly defines such an equation.

The latter creates a new mathematical representation that may not exactly reproduce BRE.

---

# 9. Create the DATA-001 files

Use the established architecture:

```text
04_ENGINEERING_DATA/
└── GRAPHS/
    └── FIGURE_04/
        ├── figure_04_metadata.md
        ├── figure_04_digitization_notes.md
        └── figure_04_points.json
```

### Metadata should contain

```text
Figure number
Exact title
Source document
Edition
Printed page
PDF page
Section references
Axes
Units
Curves
Curve meanings
Valid domains
Interpolation method
Source provenance
Verification status
```

### Digitization notes

Document the actual extraction procedure.

### JSON

Store only source-supported engineering data.

No synthetic points.

---

# 10. Build the Figure 4 provider

Follow the same pattern as Figure 3.

Conceptually:

```python
get_figure_4_data(...)
```

The provider should:

1. load the verified dataset,
2. validate schema,
3. validate source status,
4. check input domain,
5. interpolate only within the verified domain,
6. block production if the dataset isn't verified.

---

# 11. Add a provenance guard

Before DATA-001 is verified:

```text
Figure 4 provider
        ↓
PENDING_ENGINEERING_VERIFICATION
        ↓
EngineeringDataUnavailableError
```

After verification:

```text
Figure 4 provider
        ↓
VERIFIED
        ↓
production calculation allowed
```

This is especially important because you already have Figure 5 in:

```text
SYNTHETIC_PLACEHOLDER
```

and Figure 6 in:

```text
PENDING_USER_DATA
```

The production engine must never accidentally consume either.

---

# 12. Test Figure 4 independently

Create:

```text
tests/calculations/test_figure_04.py
```

or whatever naming convention your project currently uses.

Test:

### A. Data schema

```text
dataset loads
required fields exist
units correct
curve identifiers valid
```

### B. Domain

```text
lower boundary
upper boundary
inside domain
outside domain
```

### C. Interpolation

Use **known digitized points**:

```text
point itself → exact value
midpoint → expected interpolation
```

Do not test against invented expected values.

### D. Production blocking

Verify:

```text
unverified Figure 4
→ EngineeringDataUnavailableError
```

---

# 13. Create a source-evidence test

This is different from a normal unit test.

For selected points from the graph:

```text
source graph
     ↓
digitized point
     ↓
provider result
```

Verify the provider reproduces the stored source point.

This creates a traceable chain:

```text
BR 331 Figure 4
      ↓
digitization
      ↓
JSON
      ↓
provider
      ↓
calculation
```

---

# 14. Audit the existing W/C calculation

Only **after DATA-001 is independently understood** should the agent inspect the existing W/C implementation.

Find:

```text
src/bre_engine/
```

for modules dealing with:

* strength → W/C
* free-water/cement ratio
* maximum W/C
* W/C constraints

Then compare the current architecture with:

```text
fm
 ↓
Figure 4
 ↓
strength-derived W/C
 ↓
optional maximum W/C
 ↓
final W/C
```

Do not rewrite it immediately.

First produce an audit.

---

# 15. Resolve the Figure 4 input variable

This deserves special attention.

The agent must prove whether Figure 4 uses:

$$
f_m
$$

or:

$$
f_c
$$

as its strength input.

Given the preceding BRE procedure, it is likely connected to target mean strength, but **do not rely on memory or assumptions**.

The graph's axis label and Section 5.1 wording must determine this.

This affects the entire chain.

---

# 16. Determine the interpolation rule

There are several possibilities:

```text
linear interpolation
step selection
nearest curve
bilinear interpolation
manual graph reading
```

Do not automatically use linear interpolation just because it is convenient.

If the source simply provides a graph, document:

> Software interpolation method used to reproduce graph reading.

Then quantify/document the limitation if appropriate.

For example:

```text
BRE source:
graphical lookup

Software:
linear interpolation between verified digitized points
```

That is a software representation decision, not a BRE equation.

Keep the distinction explicit.

---

# 17. Check the existing maximum W/C architecture

The existing project decision was:

```text
max_wc_ratio = EXTERNAL_INPUT
```

Keep this.

The final calculation should conceptually be:

```text
Figure 4 result
       │
       ▼
strength-derived W/C
       │
       ├──────────────┐
       │              │
       │       external max W/C
       │              │
       └──────┬───────┘
              ▼
       minimum of both
              │
              ▼
        final W/C
```

Don't import exposure-class tables from BS 8110 unless the project scope explicitly expands to them.

---

# 18. Test the complete Stage 2 chain

Once Figure 4 and the W/C logic are ready:

```text
EQ-BRE-001
      ↓
target mean strength
      ↓
Figure 4
      ↓
strength-derived W/C
      ↓
maximum W/C constraint
      ↓
final W/C
```

Test at least:

```text
no maximum W/C
maximum W/C > calculated W/C
maximum W/C < calculated W/C
maximum W/C = calculated W/C
```

Again, the actual numerical expected values should come from BR 331 examples or verified Figure 4 points.

---

# 19. Compare against BR 331 examples

This is your strongest validation opportunity.

Search the PDF for worked examples containing:

* target mean strength
* free-water/cement ratio
* Figure 4
* maximum W/C
* final W/C

Extract the exact example inputs and outputs.

Then reproduce:

```text
BR 331 example
       ↓
software
       ↓
comparison
```

Create a validation table:

| Quantity             | BRE example | Software | Difference | Status |
| -------------------- | ----------: | -------: | ---------: | ------ |
| \(f_m\)              |             |          |            |        |
| strength-derived W/C |             |          |            |        |
| max W/C              |             |          |            |        |
| final W/C            |             |          |            |        |

Do not fabricate values where the source does not provide them.

---

# 20. Regression test

Then run:

```powershell
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

The target is:

```text
65 existing tests
+
Figure 4 tests
+
W/C integration tests
=
all passing
```

The exact final number will depend on how many tests are added.

---

# 21. Update the readiness register

After the investigation:

### If digitization is successful

```text
DATA-001 = VERIFIED
```

### If reliable digitization is impossible

```text
DATA-001 = PENDING_ENGINEERING_VERIFICATION
```

**Do not downgrade it to synthetic data just to make the pipeline run.**

If Figure 4 remains unverified, production Stage 2 must remain blocked.

---

# 22. Do not proceed to Figure 5 yet

Even if Figure 4 becomes verified, stop after completing its calculation block.

Your next dependency is:

```text
Figure 4
   ↓
W/C
   ↓
EQ-BRE-003
```

Then verify that chain.

Only after that should you start DATA-002 / Figure 5.

The full roadmap remains:

```text
                    BR 331
                      │
                      ▼
              Figure 3 VERIFIED
                      │
                      ▼
               EQ-BRE-001
                      │
                      ▼
              Target mean strength
                      │
                      ▼
              ┌───────────────┐
              │   Figure 4   │
              │    DATA-001  │
              └───────┬───────┘
                      ▼
                 Final W/C
                      │
                      ▼
                 EQ-BRE-003
                      │
                      ▼
                 Cement
                      │
                      ▼
              ┌───────────────┐
              │   Figure 5   │
              │    DATA-002  │
              └───────┬───────┘
                      ▼
                Wet density
                      │
                      ▼
                 Aggregate
                      │
                      ▼
              ┌───────────────┐
              │   Figure 6   │
              │    DATA-003  │
              └───────┬───────┘
                      ▼
              Fine aggregate
                      │
                      ▼
              Coarse aggregate
                      │
                      ▼
                  Final mix
```

# Send this to the coding agent now

Proceed with the next engineering-data gate: DATA-001 / Figure 4.

Do NOT modify EQ-BRE-001, DATA-004 Figure 3, Figure 5, or Figure 6 unless a concrete dependency/provenance defect is discovered. The current baseline is:

DATA-004 Figure 3 = VERIFIED
EQ-BRE-001 = IMPLEMENTED_PENDING_VERIFICATION
DATA-001 Figure 4 = PENDING_ENGINEERING_VERIFICATION
DATA-002 Figure 5 = SYNTHETIC_PLACEHOLDER
DATA-003 Figure 6 = PENDING_USER_DATA
Current regression suite = 65 tests passing

The objective is to independently establish Figure 4 as verified engineering data and then audit/integrate the existing W/C calculation. Do not jump directly into implementation.

### PHASE 1: SOURCE INVESTIGATION

1. Locate the exact Figure 4 in the BR 331 source PDF.

Record:

* exact figure number
* exact title
* BR 331 edition
* printed page
* PDF page
* all relevant Section references

1. Inspect the complete surrounding text, especially the section describing selection of the target free-water/cement ratio.

Determine exactly:

* what quantity is used as the input to Figure 4
* whether it is characteristic strength fc or target mean strength fm
* units
* concrete age/strength basis
* any aggregate/cement assumptions
* any curve-selection conditions
* any limitations or notes
* how the resulting W/C ratio is used downstream

Do not assume the input variable from memory. Verify it from the source.

### PHASE 2: FIGURE STRUCTURE

1. Identify every axis:

* axis label
* units
* numerical range
* scale
* tick spacing

1. Identify every curve in Figure 4.

For each curve document:

* curve identifier
* engineering meaning
* curve parameter
* units
* source evidence

Do not invent curve meanings.

1. Determine the valid domain of the graph.

Record:

* minimum/maximum strength
* minimum/maximum W/C
* any other applicable bounds

Do not permit software extrapolation unless BR 331 explicitly supports it.

### PHASE 3: DIGITIZATION

1. Render the actual Figure 4 at high resolution.

2. Digitize ONLY from the actual source graphic or explicitly printed source values.

Do NOT:

* fit a synthetic exponential/polynomial curve
* generate values from physical assumptions
* replace missing source data with engineering estimates
* silently extrapolate

The previous Figure 4 synthetic-data issue must not be repeated.

1. Use calibrated pixel-to-engineering-coordinate extraction.

Document:

* source image
* image resolution
* graph bounds
* calibration points
* pixel-to-value transformation
* extracted points
* curve identifiers
* interpolation method
* extraction uncertainty/limitations

1. Determine whether the graph should be represented as:

* discrete lookup points
* piecewise interpolation
* another source-supported representation

Do not convert the graph into a new analytical equation unless BR 331 explicitly provides one.

### PHASE 4: ENGINEERING DATA FILES

Create/update:

04_ENGINEERING_DATA/GRAPHS/FIGURE_04/figure_04_metadata.md
04_ENGINEERING_DATA/GRAPHS/FIGURE_04/figure_04_digitization_notes.md
04_ENGINEERING_DATA/GRAPHS/FIGURE_04/figure_04_points.json

Metadata must include:

* exact source
* page references
* axes/units
* curves
* curve meanings
* valid domains
* interpolation/representation method
* provenance
* verification status

JSON must contain ONLY source-supported engineering data.

### PHASE 5: PROVIDER AND PROVENANCE GUARD

Create or update the Figure 4 engineering-data provider using the existing Figure 3 architecture as the pattern.

The provider must:

* load validated Figure 4 data
* validate schema
* check engineering-data status
* reject unverified data in production
* reject out-of-domain values unless explicitly supported
* perform only the documented interpolation method

If DATA-001 is not verified:

production lookup must raise EngineeringDataUnavailableError.

Synthetic test fixtures may be used only in isolated software tests and must never be treated as production engineering data.

### PHASE 6: TEST FIGURE 4 INDEPENDENTLY

Create appropriate Figure 4 tests covering:

1. schema validation
2. source metadata
3. curve identification
4. domain limits
5. exact digitized points
6. interpolation between verified points
7. boundary behavior
8. out-of-domain behavior
9. production blocking when status is unverified

Expected values must come from verified source points, not invented numbers.

### PHASE 7: AUDIT EXISTING W/C CALCULATION

After Figure 4 is independently understood, inspect the current W/C calculation modules.

Determine:

* current API
* current inputs
* current outputs
* current Figure 4 dependency
* current interpolation/lookup behavior
* current maximum W/C handling
* current rounding
* current tests
* current assumptions

Do NOT refactor immediately. First document the audit.

The intended source chain is:

target mean strength fm
↓
Figure 4
↓
strength-derived free-W/C
↓
optional externally specified maximum W/C
↓
final free-W/C

The existing project decision remains:

max_wc_ratio = EXTERNAL_INPUT

Do not import BS 8110/BS 5328 exposure tables into this project unless explicitly instructed.

### PHASE 8: MAXIMUM W/C RULE

Verify against the BR 331 text that the final W/C is:

min(strength-derived W/C, externally specified maximum W/C)

when a maximum W/C is supplied.

If no maximum W/C is supplied, retain the strength-derived W/C and explicitly record that no external maximum was specified.

Do not silently create a maximum W/C from another standard.

### PHASE 9: BR 331 WORKED EXAMPLES

Search the source PDF for worked examples involving:

* target mean strength
* Figure 4
* free-water/cement ratio
* maximum free-W/C
* final W/C

Extract exact source values.

Where a worked example provides enough information, reproduce it through the software.

Create a validation comparison:

Quantity | BR 331 example | Software | Difference | Status

Do not invent missing values.

### PHASE 10: INTEGRATION

Only after the independent Figure 4 provider is tested, integrate it into the W/C calculation.

Test:

target mean strength
↓
Figure 4
↓
strength-derived W/C
↓
maximum W/C constraint
↓
final W/C

Test at least:

* no maximum W/C
* maximum W/C greater than calculated W/C
* maximum W/C lower than calculated W/C
* maximum W/C equal to calculated W/C
* valid Figure 4 domain
* invalid/out-of-domain Figure 4 input

### PHASE 11: ROUNDING

Do not introduce arbitrary rounding.

Keep engineering calculations at full precision.

If BR 331 specifies a presentation/rounding rule for W/C, document it separately from the internal calculation.

Do not use Python round() as a substitute for a source-supported rounding rule.

### PHASE 12: REGRESSION

Run:

python -m pytest tests/

Report:

* previous count
* new count
* passed
* failed
* skipped
* changed expectations
* reason for every changed expectation

Do not modify expected values merely to make tests pass.

### PHASE 13: DOCUMENTATION

Update:

* DATA_READINESS_REGISTER.md
* Figure 4 provenance documentation
* relevant walkthrough.md
* verification matrix
* implementation/task documentation

If the graph cannot be reliably digitized, keep:

DATA-001 = PENDING_ENGINEERING_VERIFICATION

and keep production blocked.

If and ONLY if the actual source evidence and calibrated extraction are sufficient, promote:

DATA-001 = VERIFIED

### PHASE 14: STOPPING POINT

Do NOT proceed to DATA-002 / Figure 5 after this task automatically.

The stopping point is:

DATA-001 Figure 4 independently verified
↓
W/C calculation correctly integrated
↓
BR 331 examples reproduced where possible
↓
full regression passing
↓
documentation updated

Then report the result for review before moving to Figure 5.

FINAL REPORT MUST INCLUDE:

1. exact Figure 4 title
2. source location
3. surrounding Section references
4. exact input variable to Figure 4
5. axes and units
6. every curve and its meaning
7. complete valid domain
8. digitization method
9. extracted data summary
10. interpolation method
11. uncertainty/limitations
12. DATA-001 status
13. provider implementation
14. production guard behavior
15. W/C calculation audit
16. maximum W/C handling
17. rounding rule
18. BR 331 worked-example comparison
19. files created/modified
20. tests added/modified
21. complete pytest result
22. remaining engineering-verification status
23. exact recommended next step

Engineering guardrail:
No invented BRE values. No synthetic production data. No duplicate sources of truth. No unverified graph values in production. No silent assumptions. Passing software tests is not equivalent to engineering-source verification.

### One extra checkpoint I would enforce

When the agent comes back, **don't let it immediately modify the W/C engine if its Figure 4 extraction report contains uncertainty**.

The order should be:

**source → digitization → verification → provider → tests → W/C integration**

not:

**source → guessed points → code → tests pass → call it verified**.

That distinction is what will keep this BRE engine auditable all the way through the final mix design.
