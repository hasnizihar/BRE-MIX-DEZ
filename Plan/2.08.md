Yes. Now that the **normal-concrete MVP is running**, the project should move from "make the calculator work" to **MVP hardening → engineering verification → production-ready release**.

The important thing is **not to keep expanding randomly**. Freeze the current MVP first, then attack the remaining engineering gap, especially Figure 6.

# Next Part: Post-MVP Engineering & Release Workflow

```text
                    CURRENT
                       │
                       ▼
             ┌───────────────────┐
             │ MVP 2.07 COMPLETE │
             │ 79 tests passing  │
             └─────────┬─────────┘
                       │
                       ▼
              01. MVP FREEZE
                       │
                       ▼
              02. ENGINEERING QA
                       │
                       ▼
              03. FIGURE 6 DATA
                       │
                       ▼
              04. STAGE 5 REFINEMENT
                       │
                       ▼
              05. COMPLETE BRE CHAIN
                       │
                       ▼
              06. VALIDATION SUITE
                       │
                       ▼
              07. REPORT GENERATION
                       │
                       ▼
              08. UI / UX HARDENING
                       │
                       ▼
              09. RELEASE CANDIDATE
                       │
                       ▼
                 BRE MIX MVP v1
```

---

# 1. FIRST: Freeze the Current MVP

Do this **before changing anything else**.

Your current state is:

> **79 tests passing, MVP server working, Stages 1–5 connected.**

Create a proper release checkpoint.

### Create

```text
08_AI_AGENT/
└── PHASE_2/
    └── CHECKPOINT_MVP_2_07_COMPLETE.md
```

Record:

```text
Version: MVP-2.07
Date: 2026-09-20

Stages:
✓ Stage 1
✓ Stage 2
✓ Stage 3
✓ Stage 4
✓ Stage 5

Tests:
79 passed
0 failed

Known engineering limitations:
- Figure 6 is pending user data
- Fine aggregate proportion currently requires user input
- Table 3 is MVP hardcoded
- Normal concrete scope only
- No air-entrained workflow
```

Then:

```bash
git add .
git commit -m "release: BRE mix design MVP 2.07"
```

**Do not rewrite the existing pipeline during this step.**

---

# 2. Run a Real MVP Engineering QA

The next question isn't:

> "Can the code run?"

It is:

> **"Can the MVP be trusted not to silently produce an invalid engineering result?"**

Create:

```text
08_AI_AGENT/PHASE_2/
└── MVP_ENGINEERING_QA.md
```

Test these categories.

### A. Valid input

```text
fck = 30
age = 28
defective = 5%
```

Expected:

```text
calculation succeeds
```

### B. Missing required input

```text
fck = null
```

Expected:

```text
validation error
```

### C. Impossible values

Examples:

```text
fck = -10
W/C = -0.2
cement = -100
fine proportion = 120%
```

Expected:

```text
rejected
```

### D. Figure 4 boundaries

Test:

```text
datum = 3
datum = 7
datum = 13
datum = 20
datum = 30
datum = 40
datum = 50
datum = 60
datum = 70
```

No extrapolation should silently occur outside the verified domain.

### E. Figure 5 boundaries

Same principle.

### F. Figure 6 absence

This is particularly important.

The system must say something like:

```text
Fine aggregate proportion is required.

Figure 6 data has not yet been verified.
Please provide the required engineering input.
```

It must **not** quietly invent a value.

---

# 3. Then Attack DATA-003 / Figure 6

This is now your biggest engineering-data gap.

You already have:

```text
DATA-004 Figure 3     ✓
DATA-001 Figure 4     ✓
DATA-002 Figure 5     ✓
DATA-003 Figure 6     PENDING_USER_DATA
```

So the next major block is:

# **Figure 6: Fine Aggregate Percentage**

Workflow:

```text
BR 331 source
      ↓
Locate Figure 6
      ↓
Render source page
      ↓
Identify axes
      ↓
Identify every curve
      ↓
Identify variables controlling curves
      ↓
Determine valid domain
      ↓
Digitize points
      ↓
Create provenance JSON
      ↓
Independent verification
      ↓
Test Example 1
      ↓
Integrate
```

Directory:

```text
04_ENGINEERING_DATA/
└── GRAPHS/
    └── FIGURE_06/
        ├── figure_06_points.json
        ├── figure_06_metadata.md
        └── figure_06_digitization_notes.md
```

---

# 4. Do NOT Let the Agent Guess Figure 6

This is the most important guardrail for the next phase.

Give the agent this instruction:

> **Investigate DATA-003 / Figure 6 from the original BR 331 source. Do not generate or assume fine aggregate percentages. Do not use typical concrete values as substitutes. Do not fit synthetic curves. First identify exactly what Figure 6 depends on, all plotted curves, axes, units, valid ranges, and the values required by Stage 5. If the graph cannot be reliably digitized from the available source, stop and mark the data as PENDING_USER_DATA rather than inventing values.**

That preserves the architecture you've spent all this time building.

---

# 5. Rebuild Stage 5 Around Figure 6

Currently your MVP has:

```text
Fine aggregate proportion
        ↑
user supplied
```

After Figure 6 is verified, you want:

```text
Design parameters
       │
       ▼
   Figure 6
       │
       ▼
Fine aggregate %
       │
       ▼
Fine aggregate mass
       │
       ▼
Coarse aggregate mass
```

Then the user should **not need to manually enter fine aggregate percentage** for supported Figure 6 conditions.

---

# 6. Create a Complete Stage 1–5 Golden Test

You already have the Example 1 golden test.

Now make it the project's primary acceptance test.

Something like:

```text
tests/
└── golden_cases/
    ├── test_br331_example_1.py
    ├── test_stage_1_5_complete.py
    └── test_boundary_cases.py
```

The complete test should verify:

```text
Input
 ↓
Target Mean Strength
 ↓
W/C
 ↓
Free Water
 ↓
Cement
 ↓
Wet Density
 ↓
Total Aggregate
 ↓
Fine Aggregate %
 ↓
Fine Aggregate
 ↓
Coarse Aggregate
```

And validate every intermediate result.

---

# 7. Separate "Exact Engineering Calculation" From "Displayed Result"

You've already started doing this correctly.

Keep the architecture:

```text
ENGINE
│
├── exact floating-point calculation
│
└── provenance
```

then:

```text
PRESENTATION
│
├── nearest 5 kg
├── significant figures
└── display formatting
```

For example:

```text
Internal:
Fine aggregate = 516.42 kg/m³

Presentation:
Fine aggregate = 515 kg/m³
```

Never let:

```python
round(...)
```

creep into the mathematical equation merely because the BRE table displays rounded values.

---

# 8. Build the Final Calculation Report

Your current trace is good for debugging.

Now turn it into a proper engineering report.

Example:

```text
BRE CONCRETE MIX DESIGN REPORT

Project:
[Project Name]

Design strength:
30 N/mm²

Age:
28 days

────────────────────────────

1. TARGET MEAN STRENGTH

Characteristic strength     30.0
Standard deviation           8.0
Margin                      13.12
Target mean                 43.12 N/mm²

────────────────────────────

2. WATER/CEMENT RATIO

Target mean strength        43.12
Strength-derived W/C        ...
Specified maximum W/C       ...
Adopted W/C                 ...

────────────────────────────

3. CEMENT

Free water                  ...
W/C                         ...
Calculated cement           ...
Adopted cement              ...

────────────────────────────

4. WET DENSITY

Wet density                 ...

────────────────────────────

5. AGGREGATES

Total aggregate             ...
Fine aggregate              ...
Coarse aggregate            ...

────────────────────────────

FINAL MIX

Water                        ...
Cement                       ...
Fine aggregate               ...
Coarse aggregate             ...
```

And below every result:

**Source / equation / input trace**

---

# 9. Add Export

Once the report works in HTML, add:

```text
[ Export PDF ]
```

The PDF should contain:

* project information
* input parameters
* calculation steps
* final mix quantities
* units
* source references
* calculation trace
* assumptions
* warnings
* software version

This turns the project from a **calculator demo** into something much closer to an engineering tool.

---

# 10. Add a "Data Status" Panel

This would be particularly useful for your architecture.

Something like:

| Data               | Status            |
| ------------------ | ----------------- |
| Figure 3           | VERIFIED          |
| Figure 4           | VERIFIED          |
| Figure 5           | VERIFIED          |
| Figure 6           | PENDING USER DATA |
| Table 3            | MVP SOURCE DATA   |
| Table 7            | VERIFIED          |
| Standard deviation | VERIFIED          |

This prevents users from assuming every part of the BRE procedure is equally verified.

---

# 11. Add Calculation Warnings

For example:

```text
⚠ Figure 6 data unavailable

The fine aggregate proportion has not been automatically
determined because the required engineering data has not
been verified.

Manual engineering input is required.
```

And:

```text
⚠ Outside verified domain

The requested target strength is outside the verified
Figure 4 range.

No extrapolation has been performed.
```

This is much better than producing a suspiciously precise number.

---

# 12. Then Do a "No Fake Data" Audit

Search the entire codebase for:

```text
0.47
340
2400
513
515
1385
1900
```

and determine whether each occurrence is:

* source data
* test fixture
* Example 1 reference
* engineering constant
* default
* accidental hardcoding

This is a very worthwhile audit because your MVP now contains several legitimate Example 1 values.

The goal is to ensure you don't accidentally have:

```python
fine_aggregate = 515
```

when you intended:

```python
fine_aggregate = figure_06_lookup(...)
```

---

# 13. Audit the Pipeline for Hidden Hardcoding

Your walkthrough already says:

> Table 3 is hardcoded in the pipeline for the MVP.

That's acceptable **for the MVP**, but don't let it remain invisible.

Move it into a clearly identified source-data module:

```text
src/
└── bre_engine/
    └── data/
        ├── table_2.py
        ├── table_3.py
        ├── figure_03.py
        ├── figure_04.py
        ├── figure_05.py
        └── figure_06.py
```

Then the pipeline becomes:

```text
pipeline
   ↓
provider
   ↓
verified engineering data
```

rather than:

```text
pipeline
   ↓
random embedded constants
```

---

# 14. Then Move to MVP v1.0 Release Candidate

Once Figure 6 and Stage 5 are complete:

```text
BRE MIX DESIGN
       │
       ▼
     RC-1
       │
       ├── Full regression
       ├── Golden Example
       ├── Boundary tests
       ├── Invalid input tests
       ├── Data provenance audit
       ├── UI test
       ├── PDF test
       └── No-fake-data audit
              │
              ▼
          MVP v1.0
```

---

# Your Immediate Roadmap

I would structure the next phases like this:

| Phase    | Work                                    | Priority |
| -------- | --------------------------------------- | -------: |
| **2.08** | MVP freeze + engineering QA             |       🔴 |
| **2.09** | Figure 6 source investigation           |       🔴 |
| **2.10** | Figure 6 digitization + verification    |       🔴 |
| **2.11** | Stage 5 automatic fine aggregate lookup |       🔴 |
| **2.12** | Full Stage 1–5 golden validation        |       🔴 |
| **2.13** | Calculation report/PDF                  |       🟠 |
| **2.14** | Data provenance + hardcoding audit      |       🟠 |
| **2.15** | UI/UX hardening                         |       🟠 |
| **2.16** | Release candidate QA                    |       🔴 |
| **2.17** | **BRE Mix Design MVP v1.0**             |       🟢 |

---

# One Important Correction Before You Continue

Your walkthrough says:

> **"Stages 1–5 calculation pipeline matches the structural engineering standards of BR 331 exactly."**

I would **not keep that wording** in the project's formal documentation yet.

Because you still have:

```text
Figure 6 = PENDING_USER_DATA
```

and your current MVP uses:

```text
fine aggregate proportion = user supplied
```

A safer statement is:

> **"The implemented Stages 1–5 calculation framework follows the verified BR 331 procedures and successfully reproduces the validated reference calculations within the currently supported data scope."**

That's a much more defensible engineering claim.

---

## So the next task is very clear

**Do not add another feature yet.**

Move directly to:

### **Phase 2.09: DATA-003 / Figure 6 Source Investigation**

Your coding agent should first:

1. Locate Figure 6.
2. Render the original page.
3. Extract its text/context.
4. Identify every axis.
5. Identify every curve.
6. Identify what each curve represents.
7. Identify required input variables.
8. Determine the valid domain.
9. Determine whether digitization is possible with sufficient confidence.
10. Produce `implementation_plan.md`.
11. **Stop before modifying Stage 5.**

That gives you a clean decision gate before the final missing engineering-data block.
