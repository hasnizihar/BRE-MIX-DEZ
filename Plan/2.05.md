Yes. **Do not move to Figure 5 yet.** The latest audit correctly exposed that Figure 4 is not sufficiently digitized for a general-purpose BRE mix-design engine.

The next phase should be a **DATA-001 Figure 4 completion and verification phase**, followed by a final Stage 2/3 verification. Here is the workflow I would give the coding agent.

# Next Workflow: Complete DATA-001 / Figure 4

## Phase 24A: Freeze the Current State

**Goal:** Preserve the working Stage 1 → Stage 3 implementation before touching Figure 4.

### 1. Run the baseline suite

```powershell
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

Expected:

```text
71 passed
```

Record the exact result in:

```text
08_AI_AGENT/PHASE_2/walkthrough.md
```

### 2. Verify repository state

Inspect:

```text
src/bre_engine/
tests/
04_ENGINEERING_DATA/
08_AI_AGENT/PHASE_2/
```

Do **not** modify:

* `EQ-BRE-001`
* `EQ-BRE-003`
* Stage 1 logic
* Stage 3 logic
* Figure 3 data

during the Figure 4 extraction itself.

---

# Phase 24B: Re-open BR 331 Figure 4

The agent should return to the **original BR 331 PDF**, not derive anything from the current JSON.

Relevant source:

> **Figure 4: Relationship between free-water/cement ratio and compressive strength**

The agent should identify:

* printed page
* PDF page
* figure title
* X-axis
* Y-axis
* every plotted curve
* datum values represented by the curves
* curve intersections/endpoints
* valid W/C range
* valid characteristic/target mean strength range

Create/update:

```text
04_ENGINEERING_DATA/GRAPHS/FIGURE_04/
├── figure_04_points.json
├── figure_04_metadata.md
└── figure_04_digitization_notes.md
```

---

# Phase 24C: Digitize **All** Figure 4 Curves

This is the most important step.

The previous extraction used only:

* Datum 40
* Datum 50

That is **not enough**.

The agent must identify the **nine plotted curves** and digitize each one.

Do **not** say:

> "Two curves are sufficient with interpolation."

Do **not** extrapolate beyond the digitized curves.

Do **not** synthesize intermediate curves mathematically unless BR 331 explicitly supports that interpretation.

### Required data structure

Something conceptually like:

```json
{
  "figure": "Figure 4",
  "x_axis": {
    "name": "Target mean strength",
    "symbol": "fm",
    "unit": "N/mm2"
  },
  "y_axis": {
    "name": "Free-water/cement ratio",
    "symbol": "w/c",
    "unit": "-"
  },
  "curves": [
    {
      "datum": 30,
      "points": [
        {"fm": ..., "wc": ...},
        {"fm": ..., "wc": ...}
      ]
    },
    {
      "datum": 35,
      "points": []
    }
  ]
}
```

The **actual values must come from the figure**.

Do not invent values to fill gaps.

---

# Phase 24D: Establish the Digitization Method

Because Figure 4 is graphical rather than tabulated, the agent needs to document **how each point was obtained**.

For every curve record:

* source image/PDF page
* pixel coordinate if useful
* graph coordinate
* extracted `fm`
* extracted `w/c`
* extraction precision
* whether the point is an endpoint, intersection, or sampled point

For example:

```text
Source:
BR 331, Figure 4, PDF page XX

Curve:
Datum = XX N/mm²

Point:
fm = XX N/mm²
w/c = X.XX

Extraction method:
Graph-coordinate digitization
```

This is important because the JSON is **engineering source data**, not merely application configuration.

---

# Phase 24E: Determine the Exact Curves

Before writing interpolation code, create a table:

| Curve | Datum | Digitized? | Verified? |
| ----- | ----: | ---------- | --------- |
| 1     |     ? | ☐          | ☐         |
| 2     |     ? | ☐          | ☐         |
| 3     |     ? | ☐          | ☐         |
| 4     |     ? | ☐          | ☐         |
| 5     |     ? | ☐          | ☐         |
| 6     |     ? | ☐          | ☐         |
| 7     |     ? | ☐          | ☐         |
| 8     |     ? | ☐          | ☐         |
| 9     |     ? | ☐          | ☐         |

The exact datum values must be taken from BR 331.

If a curve's datum cannot be confidently identified:

**STOP and mark it `[PENDING_ENGINEERING_VERIFICATION]`.**

Do not guess.

---

# Phase 24F: Validate Figure 4 Against BR 331 Example 1

Use the worked example as a mandatory regression point.

The known relationship from your previous audit is approximately:

```text
Target mean strength = 46 N/mm²
W/C = 0.47
```

The completed Figure 4 dataset should reproduce that point within the documented digitization tolerance.

Create a dedicated test:

```text
tests/data/test_figure_04.py
```

Something conceptually like:

```text
Example 1:
fm = 46
expected W/C ≈ 0.47
```

The tolerance must be based on **graph extraction precision**, not chosen merely to make the test pass.

Document the tolerance and why it is reasonable.

---

# Phase 24G: Test Domain Boundaries

This is critical because the previous implementation risked unsafe extrapolation.

Tests should cover:

### Inside valid domain

```text
fm inside lowest/highest curve range
```

Expected:

```text
successful interpolation
```

### Between adjacent curves

```text
fm / datum combination between two available curves
```

Expected:

```text
interpolation
```

### Exact curve

```text
fm exactly on a digitized curve
```

Expected:

```text
direct curve result
```

### Lower boundary

Test the lowest valid datum.

### Upper boundary

Test the highest valid datum.

### Outside domain

Attempt:

```text
below minimum supported curve
above maximum supported curve
```

Expected:

```text
EngineeringDataUnavailableError
```

or the project's designated engineering-domain exception.

**Never silently extrapolate.**

---

# Phase 24H: Decide the Interpolation Model

Only after the complete curves exist should the agent finalize interpolation.

The likely architecture is:

```text
target mean strength
        ↓
locate surrounding curve/datum
        ↓
interpolate along each curve
        ↓
interpolate between surrounding datum curves
        ↓
W/C
```

But the exact interpolation method must be documented.

For example:

```text
1D interpolation along curve
        +
interpolation between adjacent datum curves
```

The agent should **not** introduce polynomial fitting, regression, splines, or synthetic curve generation unless the source explicitly justifies it.

For a BRE design engine, simple transparent interpolation is preferable to a black-box fit.

---

# Phase 24I: Re-audit `lkp_bre_001.py`

Once Figure 4 is complete, inspect:

```text
src/bre_engine/calculations/lkp_bre_001.py
```

Confirm that it:

1. obtains Figure 4 data through `EngineeringDataProvider`
2. does not contain hardcoded Figure 4 values
3. does not contain fallback curves
4. does not extrapolate
5. records provenance
6. respects the external maximum W/C constraint
7. returns mathematically precise values
8. leaves presentation rounding to the presentation layer

---

# Phase 24J: Verify the W/C Constraint Logic

Your existing architecture says:

```text
strength-derived W/C
        ↓
externally specified maximum W/C
        ↓
final W/C = min(strength-derived W/C,
                specified maximum W/C)
```

Test both situations.

### Case A

```text
strength-derived W/C < maximum W/C
```

Expected:

```text
strength-derived W/C
```

### Case B

```text
strength-derived W/C > maximum W/C
```

Expected:

```text
maximum specified W/C
```

The test should also prove that the constraint is applied **after** the Figure 4 lookup/interpolation.

---

# Phase 24K: Re-run Stage 1 → Stage 2 → Stage 3

After Figure 4 is verified:

```text
Stage 1
Target Mean Strength
        ↓
Figure 4
W/C
        ↓
Stage 3
Cement Content
```

Use the BR 331 Example 1 chain.

You should be able to demonstrate approximately:

```text
Characteristic strength
30 N/mm²

↓ Standard deviation / margin

Target mean strength
≈ 43 N/mm²

↓ Figure 4

W/C
≈ 0.47

↓ Stage 3

Cement
≈ 340 kg/m³
```

Keep the exact internal values separate from the rounded values printed in the BRE form.

---

# Phase 24L: Add a Full Example 1 Regression Test

Create something like:

```text
tests/integration/test_br331_example_1_stage_1_to_3.py
```

The test should verify the complete chain:

```text
fc
 ↓
standard deviation
 ↓
margin
 ↓
target mean strength
 ↓
Figure 4 W/C
 ↓
cement content
```

And verify the expected BRE example values **without forcing internal calculations to use presentation rounding**.

This becomes an important "golden path" test.

---

# Phase 24M: Update Engineering Registers

Only after the above passes, update:

```text
08_AI_AGENT/PHASE_2/DATA_READINESS_REGISTER.md
```

Change:

```text
DATA-001
PENDING_ENGINEERING_VERIFICATION
```

to:

```text
VERIFIED
```

**only if every curve has been independently verified.**

Also update:

```text
08_AI_AGENT/PHASE_2/EQUATION_VERIFICATION_MATRIX.md
```

for:

```text
LKP-BRE-001
EQ-BRE-003
```

Do not mark something verified simply because the tests pass. Tests can verify software behavior, not the truth of digitized source data.

---

# Phase 24N: Full Regression

Run:

```powershell
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

Expected:

```text
0 failed
```

Record:

* total tests
* passed
* failed
* skipped
* new tests
* Figure 4 tests
* integration tests

---

# Phase 24O: Engineering Decision Gate

At the end, generate:

```text
08_AI_AGENT/PHASE_2/
└── walkthrough.md
```

with this structure:

```markdown
# Phase 24 - DATA-001 Figure 4 Verification

## 1. Objective

## 2. Source
- BR 331
- Figure 4
- printed page
- PDF page

## 3. Figure Interpretation

## 4. Identified Curves

## 5. Digitization Method

## 6. Digitized Dataset

## 7. Interpolation Method

## 8. Valid Domain

## 9. Extrapolation Policy

## 10. Example 1 Validation

## 11. W/C Constraint Validation

## 12. Stage 1 → Stage 2 → Stage 3 Validation

## 13. Tests

## 14. Provenance

## 15. Engineering Limitations

## 16. Status

## 17. Next Dependency
```

---

# Then, and only then: Move to Stage 4

Once DATA-001 is genuinely verified, the roadmap becomes:

```text
                ┌──────────────────────┐
                │ Stage 1              │
                │ Strength / Margin    │
                │ EQ-BRE-001           │
                └──────────┬───────────┘
                           ↓
                ┌──────────────────────┐
                │ Stage 2              │
                │ W/C from Figure 4    │
                │ DATA-001             │
                └──────────┬───────────┘
                           ↓
                ┌──────────────────────┐
                │ Stage 3              │
                │ Cement Content       │
                │ EQ-BRE-003           │
                └──────────┬───────────┘
                           ↓
                 ★ VERIFIED CHAIN ★
                           ↓
                ┌──────────────────────┐
                │ Stage 4              │
                │ Wet Density          │
                │ DATA-002 / Figure 5  │
                └──────────────────────┘
```

## The key instruction to give your coding agent now

> **Do not investigate or implement Figure 5 yet. Complete DATA-001 first. The current Figure 4 dataset containing only Datum 40 and Datum 50 is insufficient for a general-purpose BRE 331 engine. Re-open the original BR 331 Figure 4, identify all nine plotted curves, digitize and provenance-document every curve, establish the valid domain and interpolation method, prohibit extrapolation, validate against Example 1, test the W/C maximum constraint, then rerun the complete Stage 1 → Stage 2 → Stage 3 integration and full regression suite. Only mark DATA-001 VERIFIED after the graphical data itself has been independently checked. Do not invent, synthesize, or silently extrapolate missing Figure 4 data.**

### After that

The **next major engineering task is DATA-002 / Figure 5**, which feeds **EQ-BRE-004, Wet Density**.

So the immediate order is:

**Complete Figure 4 → verify W/C lookup → verify Stage 1–3 chain → freeze → Figure 5 → EQ-BRE-004 → Stage 4 → Figure 6 → EQ-BRE-005/006 → final mix-design chain.**

That keeps the provenance chain clean instead of building another layer on top of a partially digitized graph.
