Yes, **next is DATA-004 / Figure 3**, but this time we need to be especially careful because the agent has made a strong claim that **Line A = 8 and Line B = 4 for ≥20 MPa**.

Those values may well be correct, but they must be verified from the actual graph/source before being encoded as engineering data.

Also, **do not refactor EQ-BRE-001 yet**. First lock down Figure 3 and the statistical rules. Then implement C1 + C2 against verified inputs.

Use this prompt:

Proceed with **DATA-004: Figure 3, Standard Deviation**.

This is a critical provenance step because DATA-004 will feed the target mean strength calculation.

### 1. Source-first investigation

Locate and inspect the actual Figure 3 in BR 331.

Record:

* Exact figure title
* Printed page
* PDF page
* Section(s) referring to the figure
* X-axis variable and units
* Y-axis variable and units
* Line A definition
* Line B definition
* Full ranges of both lines
* Any strength thresholds where the lines change
* Any notes or exceptions associated with the graph

Do NOT rely solely on the text extraction.

### 2. Verify Line A and Line B numerically

The previous audit reported:

* Line A = 8 N/mm² for `fc ≥ 20 N/mm²`
* Line B = 4 N/mm² for `fc ≥ 20 N/mm²`

These must now be verified against the **actual Figure 3 graphic and surrounding BR 331 text**.

Do not encode these values merely because they appeared in the previous audit.

Determine the complete piecewise relationship across the graph.

For example, if the graph contains different values below 20 N/mm², capture those too.

Do NOT create a dataset containing only the ≥20 MPa portion unless the source genuinely defines only that portion.

### 3. Determine what is actually data vs rule

Separate:

**Source-derived engineering data**

* Figure 3 Line A values
* Figure 3 Line B values

from:

**Engineering/software logic**

* `n < 20` → use Line A
* `n ≥ 20` → calculate standard deviation from test results
* calculated `s` cannot fall below Line B

Verify each rule directly from BR 331.

Do not encode procedural logic as graph data.

### 4. Digitization

Inspect whether Figure 3 can be reliably digitized from the source PDF.

If the graph contains simple straight/piecewise lines, use calibrated extraction from the actual image.

For every point or segment record:

* source coordinates
* engineering coordinates
* source curve/line
* calibration method
* extraction method
* uncertainty/limitations

If the values can be established directly from explicit BR 331 text rather than graph digitization, record that provenance instead.

Do NOT manufacture points.

Do NOT fit a synthetic mathematical curve to approximate the graph.

### 5. Provenance classification

Classify DATA-004 using the established terminology.

Possible outcomes:

* `VERIFIED`
* `PENDING_ENGINEERING_VERIFICATION`
* `SYNTHETIC_PLACEHOLDER`
* `EXTERNAL_INPUT`

Do not introduce a new status unless the project register explicitly requires it.

If reliable extraction is impossible, use `PENDING_ENGINEERING_VERIFICATION` and leave the production dataset blocked.

### 6. Data schema

Create/update:

`04_ENGINEERING_DATA/GRAPHS/FIGURE_03/`

with appropriate:

* `figure_03_metadata.md`
* `figure_03_digitization_notes.md`
* `figure_03_points.json`

The JSON should contain only source-derived values.

If the graph cannot be safely digitized, use an empty/blocked dataset rather than estimates.

### 7. Statistical inputs

Investigate the treatment of the following separately:

* number of test results `n`
* individual test results
* calculated standard deviation `s`
* characteristic strength `fc`
* proportion defective
* statistical constant `k`

Determine which are:

* user/project inputs
* calculated quantities
* BRE lookup data
* statistical constants

Do not hardcode `k = 1.64` or other values without documenting their source.

### 8. Important issue: standard deviation calculation

Determine whether BR 331 itself specifies the exact statistical formula used to calculate `s` from `n ≥ 20` test results.

If it does, document and implement it later.

If it does not, classify the calculation as an external/statistical input rather than inventing a formula and calling it BRE.

### 9. Do NOT refactor EQ-BRE-001 yet

For this task:

**DATA-004 first.**

Do not yet:

* rewrite `EQ-BRE-001`
* implement C1
* change rounding
* alter the target mean strength calculation
* change existing production behaviour

We will do that only after reviewing the DATA-004 findings.

### 10. Tests

For the DATA-004 provider/schema, test:

* valid source data loading
* missing/empty dataset
* provenance blocking
* no synthetic fallback
* correct Line A/B identification
* boundary strengths
* unsupported ranges

Tests must distinguish data-provider correctness from engineering verification.

### 11. Documentation

Update:

* `DATA_READINESS_REGISTER.md`
* relevant provenance/index files
* `task.md`
* `walkthrough.md`

Do not change EQ-BRE-001's verification status yet unless the audit specifically requires documenting its known deficiency.

### Final report

Return:

1. Exact Figure 3 title
2. Exact source location
3. X/Y axes and units
4. Complete Line A relationship
5. Complete Line B relationship
6. Verified values and their source
7. `n < 20` rule
8. `n ≥ 20` rule
9. Whether standard deviation calculation itself is defined by BR 331
10. Whether reliable digitization was possible
11. DATA-004 provenance status
12. Files created/modified
13. Tests and exact results
14. Confirmation production cannot consume unverified/synthetic Figure 3 data
15. Recommended next step

Critical rule:

**Do not refactor EQ-BRE-001 until DATA-004 has been independently verified.**

### One more important point

The agent called the current `EQ-BRE-001` "flawed." That's reasonable as an **audit finding**, but don't let it "fix" the code yet.

The proper sequence is:

**Figure 3 source → DATA-004 → statistical rule → C1 → C2 → rounding → tests**

rather than:

**existing code → guessed Figure 3 values → patched code**.

Once the agent returns the Figure 3 investigation, we'll know whether the `8 / 4 N/mm²` claims are genuinely source-backed and can then safely rebuild `EQ-BRE-001`.
