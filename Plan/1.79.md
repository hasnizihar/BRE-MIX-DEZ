Next: **Stage 1 / Standard Deviation (`DATA-004`)**.

I would **not jump to Stage 6 trial batching yet**. Before adding the finishing/batching layer, we should close the earlier upstream dependency around **target mean strength / margin**, because that affects the W/C selection and therefore the entire downstream mix.

The agent's three options aren't equivalent. The clean project order should be:

```text
DATA-004 Standard Deviation
        ↓
EQ-BRE-001 Margin / Target Mean Strength
        ↓
EQ-BRE-002 W/C selection
        ↓
EQ-BRE-003 Cement
        ↓
Figure 5 → Density
        ↓
EQ-BRE-004 Total Aggregate
        ↓
Figure 6 → Fine %
        ↓
EQ-BRE-005 Fine Aggregate
        ↓
EQ-BRE-006 Coarse Aggregate
        ↓
Stage 6 / Trial Mix & final quantities
```

So give the agent this:

Proceed to **DATA-004: Standard Deviation / Margin for target mean strength**.

Do NOT implement Stage 6 trial batching yet.

We need to close the upstream strength-design dependency before building the final batching/presentation layer.

### 1. Source-first investigation

Locate every relevant BR 331 provision concerning:

* Standard deviation
* Margin
* Target mean strength
* Previous test results
* Number of test results / number of results `n`
* The special treatment where fewer than 20 results are available
* The relationship between standard deviation and target mean strength

Identify:

* exact section/subsection
* printed page
* PDF page
* equation/calculation identifiers
* tables/figures
* worked examples

Do not infer the procedure from existing code.

### 2. Determine exactly what BR 331 says for n < 20

This is the critical part.

Investigate the source for cases such as:

* `n ≥ 20`
* `n < 20`
* no previous test data
* existing standard deviation available
* standard deviation estimated/assumed from the BRE procedure

Determine whether BR 331 provides:

* a numerical standard deviation table,
* a prescribed margin,
* a relationship involving coefficient of variation,
* a multiplier,
* or an external input.

Every numerical constant must have a direct source citation.

### 3. DATA-004 classification

Decide whether the required standard deviation information is:

* intrinsic BRE data,
* external engineering input,
* or source-derived lookup data.

Do NOT populate a dataset with guessed values.

If the source requires information that the user/project must supply, explicitly classify it as `EXTERNAL_INPUT`.

If there is a BRE table/graph containing values, create a provenance-controlled DATA-004 specification and determine whether reliable extraction is possible.

### 4. Investigate EQ-BRE-001

After DATA-004 is understood, trace the exact BRE calculation for:

**Target mean strength = specified characteristic strength + margin**

Verify:

* exact formula
* units
* terminology used by BR 331
* how margin is determined
* whether the margin is based on standard deviation
* any minimum/maximum or special-case provisions

Confirm the existing `EQ-BRE-001` implementation against the source.

Do NOT modify it yet unless the source audit identifies a concrete discrepancy.

### 5. Worked-example verification

Find the relevant BR 331 worked examples.

For each applicable example record:

* characteristic strength
* standard deviation / margin information
* target mean strength
* resulting calculation
* final W/C selection if the example continues into that stage

Compare the software's arithmetic with the source.

Clearly distinguish:

**algorithm verification**

from:

**verification of the engineering input/lookup data.**

### 6. Architecture

Maintain the project's existing provenance architecture.

If standard deviation is an external input, the calculation should receive it explicitly rather than hiding a default value inside the code.

If a BRE lookup is required, production must use a provenance-controlled provider.

No silent defaults.

No synthetic engineering constants.

No "reasonable" assumed standard deviation.

### 7. Tests

Create/update tests for:

* standard deviation input validation
* `n < 20`
* `n ≥ 20`
* missing required inputs
* target mean strength calculation
* exact BR 331 worked examples
* boundary conditions
* invalid values
* unit consistency

If BR 331 does not define a particular boundary condition, label it as a software/domain validation rather than a BRE requirement.

### 8. Regression

Run:

1. DATA-004 tests
2. EQ-BRE-001 tests
3. EQ-BRE-002 tests
4. affected Stage 1-3 integration tests
5. complete pytest suite

Report the exact test count.

### 9. Documentation

Update:

* `EQUATION_VERIFICATION_MATRIX.md`
* `DATA_READINESS_REGISTER.md`
* `task.md`
* `walkthrough.md`
* provenance/index documentation

Do not mark engineering verification complete merely because pytest passes.

### 10. Do NOT do yet

Do not:

* implement Stage 6 trial batching
* implement air-entrained concrete
* invent standard deviation values
* create synthetic DATA-004 values
* alter the Figure 4/5/6 provenance locks

Keep existing statuses unless source evidence justifies changing them.

### Final report

Return:

1. Exact BR 331 source locations
2. What BR 331 specifies about standard deviation
3. Exact treatment of `n < 20`
4. Exact margin/target mean strength procedure
5. DATA-004 provenance classification
6. EQ-BRE-001 audit result
7. Worked-example comparison
8. Files changed
9. Tests and exact results
10. Any corrections required
11. Remaining upstream dependencies
12. Recommended next component

Critical rule:

**Do not invent or assume standard deviation values.**

### One thing to watch

The agent previously described the current mix as having the "core volume geometry complete." That's true mathematically, but **not yet engineering-complete**, because Figures 4, 5 and 6 still have unresolved provenance.

So our priority now is to make the **upstream strength calculation auditable**, then tackle the remaining source-data gaps, and only afterward build Stage 6.

Send me the DATA-004/EQ-BRE-001 audit when the agent finishes.
