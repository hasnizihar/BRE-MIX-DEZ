Yes. **Do not move to Figure 5 yet.** The next step should be an **independent audit and verification of EQ-BRE-003 (Cement Content)**, because Figure 4 is now integrated into Stage 2 and the next dependency in the calculation chain is the cement-content calculation.

There is also one important issue to resolve first: the Figure 4 report says only **2 of 9 curves** were digitized. So `DATA-001` should **not be treated as fully verified** unless the source explicitly permits interpolation between those two curves. That uncertainty needs to remain visible in the provenance register.

# Next Workflow: EQ-BRE-003 Audit → Verification → Stage 3 Freeze

## Phase 23A: Freeze the current checkpoint

Before changing anything, have the agent create a checkpoint.

### 1. Record current state

Run:

```powershell
git status
git log -5 --oneline
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

Expected:

```text
67 passed
```

Do **not** modify existing code yet.

### 2. Inspect the current verification matrix

Read:

```text
08_AI_AGENT/PHASE_2/EQUATION_VERIFICATION_MATRIX.md
08_AI_AGENT/PHASE_2/DATA_READINESS_REGISTER.md
```

The agent should explicitly identify:

```text
DATA-001 / Figure 4
EQ-BRE-002 / W/C selection
EQ-BRE-003 / Cement content
DATA-002 / Figure 5
```

and document the exact status of each.

---

# Phase 23B: Audit EQ-BRE-003 from BR 331

This is the actual next engineering task.

## 3. Source-first investigation

Search BR 331 for:

* Cement content
* Calculation C3
* Stage 3
* Minimum cement content
* Free-water content
* Water/cement ratio
* Table 4
* Example 1
* Table 5 / relevant durability requirements

The agent should **not start by reading the Python implementation**.

First establish what BR 331 actually says.

The investigation should answer:

### A. What is the exact equation?

Likely:

$$
C = \frac{W}{W/C}
$$

where:

* \(C\) = cement content, kg/m³
* \(W\) = free-water content, kg/m³
* \(W/C\) = selected water/cement ratio

But **do not assume this until confirmed from BR 331**.

### B. What are the inputs?

Establish exactly whether EQ-BRE-003 receives:

```text
free_water
selected_w_c_ratio
```

or whether there are additional constraints.

### C. What is the output?

```text
cement content [kg/m³]
```

### D. Is there a minimum cement-content constraint?

This is extremely important.

Determine whether the BRE procedure requires:

$$
C_{\text{actual}} \geq C_{\min}
$$

and, if so:

* where \(C_{\min}\) comes from
* whether it is an external design requirement
* whether it is a BR 331 lookup
* whether it depends on exposure/environment
* whether it belongs inside EQ-BRE-003 or a separate constraint layer

**Do not bury the minimum-cement rule inside the basic equation unless the source requires that architecture.**

---

# Phase 23C: Validate against BR 331 worked examples

This should be mandatory.

For every available example involving cement content, extract:

| Variable          |   Source value |
| ----------------- | -------------: |
| Free water        |     `[SOURCE]` |
| W/C ratio         |     `[SOURCE]` |
| Calculated cement | `[CALCULATED]` |
| BR 331 cement     |     `[SOURCE]` |
| Difference        | `[CALCULATED]` |

For Example 1, the known chain currently gives approximately:

$$
W = 160\text{ kg/m}^3
$$

and

$$
W/C \approx 0.47
$$

Therefore the mathematical check would be approximately:

$$
C = \frac{160}{0.47}
$$

But **the agent must verify the actual BR 331 displayed value and rounding before declaring this a validation result.**

This distinction matters because your project has already encountered the:

> 513 vs 515 vs 1385

rounding issue.

So:

**calculation layer ≠ presentation layer.**

---

# Phase 23D: Audit the existing implementation

Only after establishing the source specification, inspect:

```text
src/bre_engine/calculations/eq_bre_003.py
```

and its tests.

Look specifically for:

### 1. Formula correctness

Does it implement the exact BR equation?

### 2. Units

Make sure:

```text
kg/m³ ÷ dimensionless = kg/m³
```

### 3. Zero W/C protection

Reject:

```text
W/C <= 0
```

because:

$$
C=\frac{W}{W/C}
$$

would be invalid.

### 4. Negative inputs

Reject physically impossible values such as:

```text
W < 0
```

### 5. Premature rounding

The function should preferably return exact/full internal precision.

Do **not** automatically do:

```python
round(cement, 1)
```

unless BR 331 explicitly requires this at the calculation stage.

### 6. Minimum cement constraint

Determine whether this belongs:

```text
inside EQ-BRE-003
```

or:

```text
Stage 3 constraint/validation layer
```

based on the source.

---

# Phase 23E: Build the verification plan

Create:

```text
08_AI_AGENT/PHASE_2/STAGE_3_EQ_BRE_003_AUDIT.md
```

Use this structure:

```markdown
# EQ-BRE-003 Audit

## 1. Source
- Document:
- Section:
- Printed page:
- PDF page:

## 2. BR 331 Equation

## 3. Inputs

## 4. Outputs

## 5. Units

## 6. Constraints

## 7. Rounding

## 8. Worked Examples

## 9. Existing Implementation Audit

## 10. Identified Deviations

## 11. Required Changes

## 12. Verification Tests

## 13. Provenance Status

## 14. Decision Gate
```

---

# Phase 23F: Implement only after the audit

If the audit finds the current implementation is wrong, then create:

```text
implementation_plan.md
```

before editing code.

The implementation should probably follow this architecture:

```text
Stage 1
  ↓
Target mean strength
  ↓
Stage 2
  ↓
Selected W/C
  ↓
Stage 3
  ↓
EQ-BRE-003
  ↓
Cement content
```

The key design principle remains:

> **Equation functions should calculate. Orchestrators should obtain and validate source data. Presentation layers should round.**

That separation has already protected you from several provenance problems.

---

# Phase 23G: Tests

Create or update:

```text
tests/equations/test_eq_bre_003.py
```

At minimum test:

### Test 1: BR 331 Example

Use exact source values.

```text
source inputs
      ↓
EQ-BRE-003
      ↓
expected mathematical result
```

### Test 2: Basic arithmetic

Example:

$$
W=160,\quad W/C=0.50
$$

Expected:

$$
C=320
$$

### Test 3: Invalid W/C

```text
W/C = 0
```

must fail.

### Test 4: Negative W/C

must fail.

### Test 5: Negative water

must fail.

### Test 6: Precision

Verify that the equation doesn't prematurely round.

### Test 7: Minimum cement rule

Only if the BR 331 audit confirms such a constraint belongs in this stage.

---

# Phase 23H: Integration test

Then test the complete upstream chain:

```text
EQ-BRE-001
Target mean strength
        ↓
EQ-BRE-002
Selected W/C
        ↓
EQ-BRE-003
Cement content
```

Create/update:

```text
tests/equations/test_integration_001_002_003.py
```

The integration test should use **verified/injected data**, not Figure 5.

That is important because:

```text
Figure 5 = DATA-002 = currently blocked
```

Therefore Stage 3 can be mathematically verified without pretending Figure 5 exists.

---

# Phase 23I: Do NOT connect Figure 5 yet

This is the critical guardrail.

Current state:

```text
DATA-002 / Figure 5
        ↓
SYNTHETIC_PLACEHOLDER
        ↓
BLOCKED
```

Keep it that way.

Do **not**:

* generate density values
* estimate the Figure 5 curve
* interpolate from assumed values
* use values from another concrete-design method
* copy numbers from Example 1 and call them Figure 5 data
* make a "temporary" production dataset

The engine should continue to raise:

```text
EngineeringDataUnavailableError
```

when production attempts to consume DATA-002.

---

# Phase 23J: Re-run the full regression suite

After implementation:

```powershell
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

Do not merely report:

> "tests passed"

Record:

```text
Before: 67 tests
After:  XX tests
Passed: XX
Failed: 0
Skipped: 0
```

Also separately run:

```powershell
python -m pytest tests/equations/test_eq_bre_003.py
python -m pytest tests/equations/test_integration_001_002_003.py
```

---

# Phase 23K: Update engineering registers

Update:

```text
08_AI_AGENT/PHASE_2/EQUATION_VERIFICATION_MATRIX.md
08_AI_AGENT/PHASE_2/DATA_READINESS_REGISTER.md
```

A possible resulting state:

| Component           | Status                                          |
| ------------------- | ----------------------------------------------- |
| DATA-004 / Figure 3 | VERIFIED                                        |
| EQ-BRE-001          | IMPLEMENTED_PENDING_VERIFICATION                |
| DATA-001 / Figure 4 | **VERIFY EXACT CURVE COVERAGE**                 |
| EQ-BRE-002          | IMPLEMENTED_PENDING_VERIFICATION                |
| EQ-BRE-003          | **VERIFIED / IMPLEMENTED_PENDING_VERIFICATION** |
| DATA-002 / Figure 5 | SYNTHETIC_PLACEHOLDER / BLOCKED                 |
| DATA-003 / Figure 6 | PENDING_USER_DATA                               |
| EQ-BRE-004          | IMPLEMENTED_PENDING_VERIFICATION                |
| EQ-BRE-005          | IMPLEMENTED_PENDING_VERIFICATION                |
| EQ-BRE-006          | IMPLEMENTED_PENDING_VERIFICATION                |

Do not upgrade a component to `VERIFIED` merely because its tests pass.

**Software verification and engineering-data verification are separate gates.**

---

# Phase 23L: Resolve the Figure 4 issue before final certification

This deserves its own mini-phase.

Your latest report says:

> 9 curves exist, but only 2 primary curves were digitized.

That means you should **not silently treat Figure 4 as fully source-complete**.

Have the agent perform:

### Figure 4 completeness audit

1. Identify every plotted curve.
2. Identify its parameter/value.
3. Determine whether each curve is actually required by the BRE procedure.
4. Determine whether the two digitized curves are sufficient for interpolation.
5. Check the browser extraction against the rendered PDF.
6. Compare every available worked example against the digitized model.
7. Determine the valid interpolation domain.
8. Determine extrapolation behavior.

If any of these remain uncertain:

```text
DATA-001 = PENDING_ENGINEERING_VERIFICATION
```

not `VERIFIED`.

This is especially important because Figure 4 is upstream of practically the entire remaining mix-design chain.

---

# Phase 23M: Final checkpoint

Create:

```text
08_AI_AGENT/PHASE_2/walkthrough.md
```

with:

```markdown
# Stage 3 Completion Checkpoint

## Source Verification

## EQ-BRE-003 Specification

## Mathematical Implementation

## Worked Example Validation

## Constraint Validation

## Integration Validation

## Figure 4 Dependency Status

## Figure 5 Dependency Status

## Test Results

## Provenance Status

## Known Limitations

## Remaining Dependencies

## Next Decision Gate
```

Then stop.

---

# The overall roadmap from here

Your project is now roughly at:

```text
                 BRE BR 331
                     │
                     ▼
          ┌─────────────────────┐
          │ Stage 1             │
          │ Strength / Margin   │
          │ DATA-004 ✓          │
          │ EQ-BRE-001          │
          └──────────┬──────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │ Stage 2             │
          │ W/C ratio           │
          │ DATA-001 ⚠          │
          │ EQ-BRE-002          │
          └──────────┬──────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │ Stage 3             │
          │ Cement content      │
          │ EQ-BRE-003 ← NEXT   │
          └──────────┬──────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │ Stage 4             │
          │ Wet density         │
          │ DATA-002 ✗          │
          │ EQ-BRE-004          │
          └──────────┬──────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │ Stage 5             │
          │ Aggregates          │
          │ DATA-003 ✗          │
          │ EQ-BRE-005/006      │
          └──────────┬──────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │ Stage 6              │
          │ Trial mixes /       │
          │ final adjustments   │
          └─────────────────────┘
```

## So, give your coding agent this next instruction

The cleanest next prompt is:

> **Proceed with Phase 23: EQ-BRE-003 source-first audit and verification. Do not modify production code initially. First inspect BR 331 and establish the exact source equation, inputs, units, constraints, minimum cement-content requirements, rounding behavior, and worked-example values. Then audit the existing EQ-BRE-003 implementation against that specification. Keep DATA-002/Figure 5 completely provenance-blocked. Also perform a completeness check on DATA-001/Figure 4 because only 2 of the 9 plotted curves were digitized. Do not upgrade DATA-001 to VERIFIED unless the source evidence demonstrates that the extracted curves and interpolation method are sufficient. Create an implementation_plan.md only after the source audit is complete. Do not implement anything until the audit and plan are reviewed. Run the existing 67-test suite before making changes and preserve the engineering provenance guardrails.**

That is the next clean gate. **Do not jump to Figure 5.** The project needs to close the Stage 3 equation and simultaneously resolve whether the current Figure 4 extraction is genuinely complete enough for engineering use.
