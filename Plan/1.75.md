Yes. Now that Figure 5 has been visually analyzed, **do not jump straight into coding yet**.

There is one important thing to do first: turn those visual findings into a **source-backed engineering specification**, then digitize the curves systematically.

The current report gives approximate readings, but those numbers should **not yet be treated as authoritative BRE data**.

### What Figure 5 is doing

It appears to be:

$$
\text{Free-water content}
\quad\longrightarrow\quad
\text{Wet density of concrete}
$$

with the required curve selected based on the **relative density of the combined aggregate**.

For example, the BRE defaults shown are:

* Crushed aggregate → relative density **2.7**
* Uncrushed aggregate → relative density **2.6**

Then the graph gives wet density, which will eventually be used to determine aggregate content.

So the eventual calculation chain is roughly:

```text
Required concrete workability
        ↓
Free-water content
        ↓
        Figure 5
        ↓
Wet density
        ↓
Cement + water + aggregate
        ↓
Aggregate content
```

### Next instruction to the agent

Good. The visual investigation has established the structure of **DATA-002 / Figure 5**.

Do NOT yet treat the reported sample readings as authoritative numerical data.

Proceed in this order:

### 1. Source specification

Inspect the BR 331 text immediately before and after Figure 5 and identify the exact procedure describing how Figure 5 is used.

Document:

* exact section
* printed page
* figure title
* input variable
* output variable
* units
* meaning of relative density of combined aggregate
* how the appropriate curve is selected
* BRE's stated defaults for crushed and uncrushed aggregates
* interpolation procedure
* graph boundaries
* rounding requirements
* how the resulting wet density is subsequently used

### 2. Curve digitization

The six curves are:

* relative density 2.4
* relative density 2.5
* relative density 2.6
* relative density 2.7
* relative density 2.8
* relative density 2.9

Do NOT create an arbitrary mathematical equation to represent them.

Digitize the actual plotted curves from the source PDF.

Use sufficient points across the graph to preserve the curvature, particularly because the curves are not perfectly straight.

Record the digitization uncertainty.

### 3. Interpolation architecture

The software should support:

```text
free_water + relative_density
             ↓
      Figure 5 curve
             ↓
      interpolated wet density
```

For relative densities between the plotted curves, interpolate between the adjacent curves.

For free-water values between digitized points, interpolate along the relevant curve.

Do not extrapolate outside the Figure 5 graph domain.

### 4. Data provenance

Create:

`04_ENGINEERING_DATA/GRAPHS/FIGURE_05/`

with appropriate:

* `figure_05_metadata.md`
* `figure_05_digitization_notes.md`
* structured curve data

Every digitized value must be clearly identified as **source-digitized**, not an official tabulated BRE value.

Use:

`DATA-002 = IMPLEMENTED_PENDING_VERIFICATION`

unless the source provides sufficient information for a stronger verification status.

### 5. Tests

Create tests for:

* all six relative-density curves
* exact/interior graph points
* interpolation between relative densities
* interpolation between water-content points
* default crushed aggregate curve
* default uncrushed aggregate curve
* lower/upper graph boundaries
* values outside the graph domain
* invalid relative density
* invalid water content

Do not invent expected numerical values. Where exact BRE benchmark values are unavailable, use source-digitized points and structural/property tests.

### 6. Important distinction

The current browser analysis reported approximate readings such as:

`RD 2.6, W=180 → density ≈2368 kg/m³`

These are useful for checking the digitization, but they are NOT yet verified BRE reference values.

Do not use the approximate readings as if they were authoritative until the digitization process has been completed and documented.

### 7. Stop point

First create the **engineering specification and digitization plan**.

Then implement the data extraction/interpolation layer and tests.

Afterward report:

* exact BR 331 source location
* Figure 5 procedure
* digitization method
* number of points per curve
* interpolation method
* files created/modified
* tests and results
* verification status
* next equation/data dependency

Keep:

`DATA-001 / Figure 4 = PENDING_ENGINEERING_VERIFICATION`

`EQ-BRE-003 = IMPLEMENTED_PENDING_VERIFICATION`

Do not declare the overall engine engineering-verified.

### Where we are now

We're actually making good progress:

**Figure 4** → W/C
**Maximum W/C** → external constraint
**EQ-003** → cement
**Figure 5** → wet density ← **currently here**

Once Figure 5 is properly implemented, the next step should become much clearer because we'll have another major piece of the aggregate calculation chain.

**So send the above to the agent and let it work on DATA-002.** When it gives you the implementation report, paste that here and I'll tell you whether it's safe to proceed to the next equation.
