Yes. **Now you have cleared the DATA-004 decision gate.** The next step is to refactor `EQ-BRE-001`, but do it in a controlled sequence. Do not touch Figures 4, 5, or 6 yet.

The important thing is that the agent's Figure 3 report has established the source data and selection logic separately. Now we turn that into an auditable calculation chain.

# Next workflow: EQ-BRE-001 refactor

## Phase 1: Freeze DATA-004

Before changing calculation code, create a clean checkpoint.

Verify:

```text
DATA-004 Figure 3
Status: VERIFIED
```

Check these files:

```text
04_ENGINEERING_DATA/GRAPHS/FIGURE_03/
├── figure_03_metadata.md
├── figure_03_digitization_notes.md
└── figure_03_points.json
```

Also verify that:

* Figure 3 values are source-backed.
* No synthetic values are being used.
* The provider exposes Figure 3 data.
* Tests pass.
* The existing 59 tests still pass.

**Do not modify these files during the EQ-BRE-001 refactor unless a genuine documentation correction is required.**

---

# Phase 2: Audit the current EQ-BRE-001

Have the agent inspect the existing implementation before editing it.

The target chain should become:

```text
Characteristic strength fc
             │
             ▼
       Figure 3 DATA-004
             │
       ┌─────┴─────┐
       ▼           ▼
    Line A       Line B
       │           │
       └─────┬─────┘
             ▼
     standard deviation s
             │
             ▼
          k × s
             │
             ▼
        Margin M
             │
             ▼
     fm = fc + M
```

The agent must first report:

### Current state

* Current function signature
* Current inputs
* Current outputs
* Whether `s` is currently accepted
* Whether `k` is currently accepted
* Whether Figure 3 is currently accessed
* Current rounding
* Existing tests
* Existing callers/dependencies

This prevents the refactor from accidentally breaking downstream modules.

---

# Phase 3: Define the correct engineering API

The clean conceptual API should be something along the lines of:

```python
calculate_target_mean_strength(
    characteristic_strength_fc,
    number_of_results,
    test_results=None,
    standard_deviation=None,
    proportion_defective=...,
)
```

But **do not let the agent invent the final signature yet**.

First determine exactly which inputs should be:

| Quantity                  | Classification                                 |
| ------------------------- | ---------------------------------------------- |
| `fc`                      | Engineering input                              |
| `n`                       | Engineering/statistical input                  |
| Previous test results     | External input                                 |
| `s`                       | Calculated from results or externally supplied |
| Figure 3 Line A/B         | BRE engineering data                           |
| `k`                       | BRE/statistical lookup                         |
| Proportion defective      | Design input                                   |
| Margin `M`                | Calculated                                     |
| Target mean strength `fm` | Calculated                                     |

A particularly useful design is to support either:

```text
raw test results
        ↓
calculate s
```

or:

```text
externally supplied s
        ↓
use s
```

But if both are allowed, the engine must define precedence and consistency checks. It should never silently choose one.

---

# Phase 4: Implement standard deviation calculation

The agent's source investigation says BR 331 provides:

$$
s=\sqrt{\frac{\sum(x-m)^2}{n-1}}
$$

This should become a **separate calculation**, not be buried inside EQ-BRE-001.

Recommended architecture:

```text
src/bre_engine/calculations/
├── standard_deviation.py
├── eq_bre_001.py
└── ...
```

Conceptually:

```text
test results
      ↓
calculate s
      ↓
standard-deviation selection
      ↓
EQ-BRE-001
```

This makes the statistical calculation independently testable.

---

# Phase 5: Implement Figure 3 selection logic

This is the heart of the refactor.

For:

```text
n < 20
```

the source rule is:

```text
s_design = Line A(fc)
```

For:

```text
n >= 20
```

the source rule is:

```text
s_calculated = calculated standard deviation

s_design = max(
    s_calculated,
    Line B(fc)
)
```

So conceptually:

```python
if n < 20:
    s_design = line_a(fc)
else:
    s_design = max(calculated_s, line_b(fc))
```

This logic should be implemented as its own function/module where possible.

For example:

```text
select_design_standard_deviation()
```

rather than putting everything directly into `eq_bre_001.py`.

---

# Phase 6: Implement the Figure 3 interpolation

The agent reports these verified relationships:

### Line A

For \(f_c < 20\):

$$
s_A = 4 + 0.2f_c
$$

For \(f_c \geq 20\):

$$
s_A = 8
$$

### Line B

For \(f_c < 20\):

$$
s_B = 2 + 0.1f_c
$$

For \(f_c \geq 20\):

$$
s_B = 4
$$

However, **do not blindly hard-code those equations just because they are mathematically convenient**.

The implementation should preferably use the verified Figure 3 data provider:

```text
Figure 3 points
      ↓
validated interpolation
      ↓
Line A / Line B
```

That preserves the provenance chain.

Also test the boundary:

```text
fc = 19.999...
fc = 20
fc > 20
```

The transition at 20 must be unambiguous.

---

# Phase 7: Implement margin calculation

Now implement:

$$
M=k s
$$

where `s` is the **selected design standard deviation**, not necessarily the raw calculated standard deviation.

That distinction is crucial.

For example:

```text
n >= 20
calculated s = 3
Line B = 4

selected s = 4

M = k × 4
```

Not:

```text
M = k × 3
```

---

# Phase 8: Implement target mean strength

Then:

$$
f_m=f_c+M
$$

Keep the internal calculation at full precision.

Do **not** do:

```python
round(fm, 1)
```

unless the source actually requires that representation.

Instead establish two separate concepts:

```text
calculation value
        ↓
full precision

BRE presentation value
        ↓
required rounding
```

---

# Phase 9: Resolve the rounding ambiguity carefully

This needs a specific source audit before coding.

The agent reported:

> Target Mean Strength to two significant figures

but Example 1 shows:

```text
M = 1.64 × 8
  = 13.12
```

then uses approximately:

```text
M = 13
fm = 30 + 13
   = 43
```

So the agent should verify whether:

1. `M` is explicitly rounded to an integer,
2. `fm` is explicitly rounded to two significant figures,
3. or the example simply presents intermediate values that way.

Do **not** infer a universal software rounding algorithm from one example.

This is important enough that I would make it a dedicated subtask.

---

# Phase 10: Refactor tests

The existing tests apparently validate the old implementation, so they need to be rewritten around the actual BRE procedure.

Create tests approximately in these groups:

### `test_standard_deviation.py`

Test:

```text
known dataset
→ expected s
```

and:

```text
n < 2
→ invalid
```

if that constraint is appropriate.

### `test_figure_03_selection.py`

Test:

```text
n < 20 → Line A
n = 19 → Line A
n = 20 → calculated s / Line B floor
n > 20 → calculated s / Line B floor
```

### `test_eq_bre_001.py`

Test:

```text
M = k × selected_s
fm = fc + M
```

### Boundary tests

Especially:

```text
fc = 0
fc < 20
fc = 20
fc > 20
```

### Minimum-bound tests

```text
calculated s < Line B
→ Line B

calculated s = Line B
→ Line B

calculated s > Line B
→ calculated s
```

---

# Phase 11: Test the BR 331 Example 1

This should become your primary regression test.

Use the actual example inputs:

```text
fc = 30 N/mm²
defective proportion = 5%
n < 20
Line A = 8 N/mm²
k = 1.64
```

Then:

$$
s=8
$$

$$
M=1.64(8)=13.12
$$

Then apply the **source-confirmed presentation rule** to obtain the reported target mean strength.

The test should distinguish:

```text
raw calculation
```

from:

```text
reported BRE value
```

That distinction will save you headaches later when the final mix-design report is generated.

---

# Phase 12: Integration test with Stage 2

Once EQ-BRE-001 passes independently, test the downstream dependency:

```text
EQ-BRE-001
     ↓
selected W/C ratio
     ↓
EQ-BRE-002
     ↓
free-water
     ↓
EQ-BRE-003
```

Make sure the new API doesn't break the W/C calculation.

This is especially important because the earlier project chain already has:

```text
Figure 4
   ↓
W/C
```

and Figure 4 is **still unverified**.

So the integration test should distinguish:

```text
software pipeline works
```

from:

```text
engineering production pipeline is verified
```

---

# Phase 13: Run full regression

Run:

```powershell
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

Expected outcome:

```text
old tests
+
new Figure 3 tests
+
new standard deviation tests
+
new EQ-BRE-001 tests
+
integration tests
```

No regression failures.

If failures occur, **do not simply update the expected values**. Determine whether:

* the old test encoded incorrect behavior,
* the new implementation is wrong,
* or the test represents an interface that should remain compatible.

---

# Phase 14: Update engineering documentation

After tests pass, update:

```text
DATA_READINESS_REGISTER.md
```

and the relevant:

```text
walkthrough.md
verification matrix
provenance documentation
task/checkpoint files
```

The final status should say something like:

```text
DATA-004
Figure 3
VERIFIED

EQ-BRE-001
IMPLEMENTED_PENDING_VERIFICATION
```

Even though the software implementation has now been tested, I would **not** immediately mark EQ-BRE-001 as engineering-verified unless the entire source-to-code chain has been independently checked.

---

# Phase 15: Commit/checkpoint

Before touching Figure 4/5/6, create a project checkpoint.

Conceptually:

```text
CHECKPOINT
──────────
DATA-004 verified
Standard deviation logic implemented
EQ-BRE-001 refactored
Tests passing
No Figure 4/5/6 changes
```

This gives you a clean rollback point.

---

# Then what?

After this checkpoint, your engineering-data situation should be:

```text
DATA-004  Figure 3
    VERIFIED
       ↓
EQ-BRE-001
    implemented
       ↓
       ────────────────

DATA-001  Figure 4
    PENDING_ENGINEERING_VERIFICATION

DATA-002  Figure 5
    SYNTHETIC_PLACEHOLDER

DATA-003  Figure 6
    PENDING_USER_DATA
```

So **the next major engineering task after EQ-BRE-001 is Figure 4**, not Figure 5 or Figure 6.

The correct order remains:

```text
Figure 3
   ↓
EQ-BRE-001
   ↓
Figure 4
   ↓
W/C calculation
   ↓
Cement
   ↓
Figure 5
   ↓
Wet density
   ↓
Total aggregate
   ↓
Figure 6
   ↓
Fine aggregate
   ↓
Coarse aggregate
   ↓
Final mix
```

## Send this to your coding agent now

Proceed with the EQ-BRE-001 refactor now that DATA-004 / Figure 3 has been independently verified.

This is an implementation phase. Do NOT investigate or modify Figures 4, 5, or 6 yet except where existing tests/integration require compatibility.

Follow this sequence strictly:

### 1. Freeze and verify DATA-004

Confirm that:

* DATA-004 Figure 3 is VERIFIED.
* figure_03_metadata.md
* figure_03_digitization_notes.md
* figure_03_points.json

are present and internally consistent.

Do not alter verified Figure 3 engineering data unless you discover a concrete source/provenance error.

Confirm the current full test count before refactoring.

### 2. Audit the existing EQ-BRE-001 implementation BEFORE editing

Document:

* current function signature
* current inputs
* current outputs
* current standard-deviation handling
* current k handling
* current Figure 3 dependency, if any
* current rounding behavior
* current callers/dependencies
* current tests
* current assumptions that are not supported by BR 331

Do not modify code until this audit is recorded.

### 3. Separate the engineering/statistical layers

Implement the following conceptual chain:

characteristic strength fc
↓
Figure 3 DATA-004
↓
standard deviation selection
↓
selected design standard deviation s
↓
M = k × s
↓
fm = fc + M

Keep these responsibilities separated rather than putting all logic into one function.

### 4. Implement standard deviation calculation separately

BR 331 states:

s = sqrt(sum((x - m)^2) / (n - 1))

Implement this as a separately testable calculation/module.

Determine the exact source definition of m/mean from BR 331 before implementation.

Support raw test results as an input where appropriate.

If the API also accepts a pre-calculated external s, do not silently prefer one source over another. Define and document the precedence/validation rule.

Do not import a different statistical convention without documenting and verifying it against BR 331.

### 5. Implement Figure 3 selection logic

For n < 20:

```
selected_s = Line A(fc)
```

For n >= 20:

```
calculated_s = standard deviation of the available test results
selected_s = max(calculated_s, Line B(fc))
```

Use the verified DATA-004 provider for Line A and Line B.

Do not duplicate Figure 3 engineering data in EQ-BRE-001.

### 6. Preserve the verified Figure 3 relationship

The verified Figure 3 relationships are:

Line A:
fc < 20:
s = 4 + 0.2 fc
fc >= 20:
s = 8

Line B:
fc < 20:
s = 2 + 0.1 fc
fc >= 20:
s = 4

However, implement these through the verified Figure 3 data/provider architecture rather than creating a second hard-coded source of truth.

Test the boundary at fc = 20 explicitly.

### 7. Implement margin calculation

Use:

```
M = k × selected_s
```

where selected_s is the standard deviation selected by the Figure 3/n-results procedure.

Do NOT accidentally use the raw calculated s when the Line B minimum governs.

### 8. Implement target mean strength

Use:

```
fm = fc + M
```

Keep internal engineering calculations at full precision.

Do not retain the current unconditional one-decimal rounding unless it is explicitly supported by the BR 331 source.

### 9. Resolve rounding from the source BEFORE finalizing the implementation

Perform a focused source check of the BR 331 wording and Example 1 to determine:

* how M is presented/rounded
* how fm is presented/rounded
* what “two significant figures” applies to
* whether the Example 1 use of M = 13 from 13.12 is a required rule or simply presentation

Do not infer a general rounding rule from the example alone.

Separate:

```
engineering calculation value
```

from:
BRE presentation/reporting value

### 10. Refactor EQ-BRE-001 only after the above is established

The final implementation must have clear provenance for:

* fc
* n
* test results / s
* Figure 3
* k
* proportion defective
* M
* fm

Use appropriate project statuses and do not turn assumptions into BRE source data.

### 11. Rewrite/add tests

Create or update tests covering:

A. Standard deviation:

* known dataset
* correct n-1 denominator
* mean calculation
* invalid insufficient data cases where applicable

B. Figure 3 selection:

* n < 20 → Line A
* n = 19 → Line A
* n = 20 → calculated s subject to Line B floor
* n > 20 → calculated s subject to Line B floor
* calculated s < Line B
* calculated s = Line B
* calculated s > Line B
* fc below 20
* fc = 20
* fc above 20

C. EQ-BRE-001:

* M = k × selected_s
* fm = fc + M

D. BR 331 Example 1:

* fc = 30 N/mm²
* 5% defective
* n < 20
* Line A = 8 N/mm²
* k = 1.64
* M = 13.12 before presentation rounding
* verify the final reported fm against the exact source-supported presentation rule

E. Invalid inputs:

* impossible fc
* invalid n
* invalid standard deviation
* missing test results when calculated s is required
* invalid k/proportion defective
* any other constraints explicitly supported by the source

### 12. Integration

Verify that the refactored EQ-BRE-001 still integrates with the downstream W/C calculation and existing pipeline.

Remember:

Software integration passing does NOT mean Figure 4 is engineering-verified.

Figure 4 remains:

```
DATA-001 = PENDING_ENGINEERING_VERIFICATION
```

and must continue to block production wherever its engineering data is required.

### 13. Full regression

Run:

python -m pytest tests/

Report:

* previous test count
* new test count
* passed
* failed
* skipped
* any changed expectations
* why each changed expectation was necessary

Do not simply modify expected values to make tests pass.

### 14. Documentation

Update:

* DATA_READINESS_REGISTER.md
* EQ-BRE-001 provenance/verification documentation
* relevant walkthrough.md
* verification matrix
* task/checkpoint documentation

Keep DATA-004 = VERIFIED.

Do not mark EQ-BRE-001 engineering-verified merely because software tests pass. Distinguish implementation verification from engineering/source verification.

### 15. Checkpoint

Create a clean checkpoint after the refactor:

DATA-004 Figure 3 = VERIFIED
EQ-BRE-001 = IMPLEMENTED_PENDING_VERIFICATION
Standard deviation calculation = implemented/tested
Full test suite = passing
Figures 4/5/6 = unchanged

FINAL REPORT MUST INCLUDE:

1. EQ-BRE-001 before/after behavior
2. final API/signature
3. standard deviation implementation
4. Figure 3 integration
5. n < 20 logic
6. n >= 20 logic
7. Line B minimum enforcement
8. k/proportion-defective handling
9. M calculation
10. fm calculation
11. exact rounding rule and source evidence
12. Example 1 result
13. files changed
14. tests added/changed
15. full pytest result
16. remaining engineering-verification status
17. exact next recommended task

Engineering guardrail:
Never invent BRE values, never create duplicate sources of truth, never silently replace source rules with assumptions, and never equate passing software tests with engineering verification.

**After the agent returns that report, paste it here.** The next decision gate will be whether `EQ-BRE-001` correctly reproduces the BRE procedure and rounding, after which we can move cleanly into **DATA-001 / Figure 4**.
