Now that **DATA-001 / Figure 4 is marked VERIFIED and the Stage 1 → 3 chain has 76/76 tests passing**, the next logical block is **Stage 4: Wet Density from Figure 5**.

But I would **not immediately code EQ-BRE-004**. Follow the same source-first pattern used for Figures 3 and 4.

# Next Workflow: Stage 4 / DATA-002 / Figure 5

## Phase 25A: Freeze the Verified Stage 1–3 Chain

First establish a clean checkpoint.

### 1. Run the complete suite

```powershell
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

Expected baseline:

```text
76 passed
0 failed
```

Record this in:

```text
08_AI_AGENT/PHASE_2/walkthrough.md
```

### 2. Verify the current registers

Inspect:

```text
08_AI_AGENT/PHASE_2/DATA_READINESS_REGISTER.md
08_AI_AGENT/PHASE_2/EQUATION_VERIFICATION_MATRIX.md
```

You should have approximately:

```text
DATA-004 / Figure 3     VERIFIED
DATA-001 / Figure 4     VERIFIED

EQ-BRE-001              IMPLEMENTED_PENDING_VERIFICATION
LKP-BRE-001             VERIFIED
EQ-BRE-003              IMPLEMENTED_PENDING_VERIFICATION
```

Do **not** change any of the verified Figure 3 or Figure 4 datasets.

---

# Phase 25B: Source Audit Figure 5

Now locate the original:

> **Figure 5: Relationship between wet density and free-water/cement ratio / aggregate characteristics**

Do not rely on the existing software implementation or any placeholder data.

Search the original BR 331 PDF.

Use:

```powershell
python -c "
import pymupdf
doc = pymupdf.open('feismo.com-design-of-normal-concrete-mixes-bre-pr_dc6fbedff9ab49768ff1946e8a1d8579.pdf')

for i, page in enumerate(doc):
    text = page.get_text()
    if 'Figure 5' in text:
        print(f'--- PDF PAGE {i+1} ---')
        print(text)
"
```

Then inspect the surrounding pages manually.

The objective is to determine exactly:

1. Figure 5 title
2. printed page number
3. PDF page number
4. X-axis variable
5. Y-axis variable
6. units
7. every curve
8. curve labels
9. interpolation variables
10. valid domain
11. whether any adjustment factors are applied
12. Example 1 validation point

---

# Phase 25C: Understand the Engineering Meaning Before Digitizing

This is especially important for Figure 5.

Create:

```text
08_AI_AGENT/PHASE_2/STAGE_4_EQ_BRE_004_AUDIT.md
```

Document:

```markdown
# Stage 4 EQ-BRE-004 Audit

## Source
## Figure 5
## Section 5.4
## Input Variables
## Output Variable
## Units
## Graph Interpretation
## Curve Families
## Interpolation
## Domain
## Rounding
## Example 1
## External Constraints
## Provenance
## Open Questions
```

The key question is:

> **What exactly determines wet density in BR 331?**

Do not assume the answer from the existing code.

---

# Phase 25D: Inspect Existing EQ-BRE-004

Now inspect:

```text
src/bre_engine/calculations/eq_bre_004.py
```

Also inspect:

```text
tests/equations/
tests/components/
src/bre_engine/data/provider.py
src/bre_engine/data/json_provider.py
```

Determine whether the existing implementation is:

* correct
* incomplete
* using fake Figure 5 data
* using hardcoded values
* performing interpolation
* performing rounding prematurely
* bypassing provenance controls

The audit should compare:

```text
BR 331
   ↓
actual equation/procedure
   ↓
current EQ-BRE-004
```

Do not modify the implementation yet.

---

# Phase 25E: Identify DATA-002

Create the engineering data directory:

```text
04_ENGINEERING_DATA/GRAPHS/FIGURE_05/
```

Expected structure:

```text
FIGURE_05/
├── figure_05_points.json
├── figure_05_metadata.md
└── figure_05_digitization_notes.md
```

Initially:

```json
{
  "dataset": "FIGURE_05",
  "status": "PENDING_ENGINEERING_VERIFICATION"
}
```

Do **not** insert guessed values.

---

# Phase 25F: Digitize Figure 5

Use the same procedure that worked for Figure 4.

### 1. Render the source page

For example:

```powershell
python scratch/render_pdf.py `
  feismo.com-design-of-normal-concrete-mixes-bre-pr_dc6fbedff9ab49768ff1946e8a1d8579.pdf `
  <PAGE>
```

Then render at high resolution if required:

```python
pix = page.get_pixmap(dpi=300)
pix.save("page_fig5.png")
```

### 2. Identify every plotted curve

Create a table:

| Curve | Label | Digitized | Verified |
| ----- | ----- | --------- | -------- |
| 1     | ?     | ☐         | ☐        |
| 2     | ?     | ☐         | ☐        |
| 3     | ?     | ☐         | ☐        |
| ...   | ...   | ☐         | ☐        |

**Do not repeat the Figure 4 problem of stopping after only a couple of curves.**

If Figure 5 has multiple curve families, identify them separately.

---

# Phase 25G: Store Raw Graph Data

The JSON should contain the actual extracted engineering data.

For example, structurally:

```json
{
  "dataset": "FIGURE_05",
  "status": "PENDING_ENGINEERING_VERIFICATION",
  "source": {
    "document": "BR 331",
    "figure": "Figure 5",
    "printed_page": null,
    "pdf_page": null
  },
  "x_axis": {
    "label": "",
    "unit": ""
  },
  "y_axis": {
    "label": "",
    "unit": ""
  },
  "curves": []
}
```

The exact schema should follow what Figure 5 actually contains.

---

# Phase 25H: Establish the Valid Domain

This is mandatory.

For every curve determine:

```text
minimum input
maximum input
minimum output
maximum output
```

The interpolation engine must distinguish:

```text
inside graph domain
```

from:

```text
outside graph domain
```

Outside the domain:

```text
EngineeringDataUnavailableError
```

or the project's established equivalent.

### Absolutely prohibit

```text
silent extrapolation
```

No:

```python
if x > max_x:
    use max_x
```

No:

```python
if missing:
    return default_density
```

No synthetic curve.

No arbitrary fallback.

---

# Phase 25I: Validate Figure 5 Against BR 331 Example 1

This should be the primary source-validation test.

The BR 331 worked example should provide enough information to reconstruct the Figure 5 lookup.

Trace:

```text
Stage 1
Target mean strength
       ↓
Figure 4
W/C
       ↓
free-water content
       ↓
Figure 5
wet density
```

Then compare the engine's wet-density result with the **published example value**.

This is far stronger than testing arbitrary numbers.

Create:

```text
tests/data/test_figure_05.py
```

and include the worked-example point.

---

# Phase 25J: Verify EQ-BRE-004

Only after DATA-002 has been digitized and validated should you implement/refactor:

```text
src/bre_engine/calculations/eq_bre_004.py
```

The architecture should be:

```text
Figure 5
    ↓
EngineeringDataProvider
    ↓
lookup/interpolation
    ↓
EQ-BRE-004
    ↓
Wet density
```

The equation/calculation layer should **not know where Figure 5 came from**.

This is the same provenance separation you established for:

```text
EQ-BRE-005
EQ-BRE-006
```

---

# Phase 25K: Preserve Precision

Keep the internal calculation unrounded.

For example:

```text
Figure 5 lookup
       ↓
exact/interpolated wet density
       ↓
Stage 5
```

Do not prematurely convert it to:

```text
2400 kg/m³
```

unless BR 331 explicitly requires that at that particular calculation stage.

Presentation rounding belongs later.

---

# Phase 25L: Build the Stage 4 Tests

At minimum test:

### Normal lookup

```text
valid inputs → valid wet density
```

### Exact curve point

```text
known graph point → expected value
```

### Interpolation

```text
between two graph points → interpolated value
```

### Lower boundary

```text
minimum supported value
```

### Upper boundary

```text
maximum supported value
```

### Outside domain

```text
invalid/outside graph → EngineeringDataUnavailableError
```

### Missing DATA-002

Production must stop cleanly if:

```text
DATA-002 = PENDING_USER_DATA
```

or equivalent pending state.

---

# Phase 25M: Integrate Stages 1–4

Build:

```text
tests/equations/test_integration_001_002_003_004.py
```

The complete chain should become:

```text
┌───────────────────────┐
│ Characteristic        │
│ strength fc           │
└──────────┬────────────┘
           ↓
┌───────────────────────┐
│ Standard deviation    │
│ Figure 3              │
└──────────┬────────────┘
           ↓
┌───────────────────────┐
│ Margin / Target mean  │
│ EQ-BRE-001            │
└──────────┬────────────┘
           ↓
┌───────────────────────┐
│ W/C                   │
│ Figure 4              │
└──────────┬────────────┘
           ↓
┌───────────────────────┐
│ Free water            │
│ Stage 2               │
└──────────┬────────────┘
           ↓
┌───────────────────────┐
│ Cement                │
│ EQ-BRE-003            │
└──────────┬────────────┘
           ↓
┌───────────────────────┐
│ Wet density           │
│ Figure 5 / EQ-BRE-004 │
└───────────────────────┘
```

At this point, the aggregate calculation has **not yet happened**.

---

# Phase 25N: Reconnect to Stage 5 Carefully

After Stage 4 is verified, test the handoff:

```text
Wet density
+
cement
+
free water
       ↓
EQ-BRE-005
       ↓
Fine aggregate
       ↓
EQ-BRE-006
       ↓
Coarse aggregate
```

But remember:

```text
DATA-003 / Figure 6
        ↓
PENDING_USER_DATA
```

Therefore the production pipeline should still **stop before fine aggregate calculation** if Figure 6 is unavailable.

This is an important guardrail.

---

# Phase 25O: Update the Registers

Only after successful source and software verification:

### `DATA_READINESS_REGISTER.md`

Update:

```text
DATA-002
```

from:

```text
SYNTHETIC_PLACEHOLDER
```

or whatever its current status is, to:

```text
VERIFIED
```

**only if the graph itself has been verified.**

### `EQUATION_VERIFICATION_MATRIX.md`

Update:

```text
EQ-BRE-004
```

to:

```text
IMPLEMENTED_PENDING_VERIFICATION
```

or:

```text
VERIFIED
```

depending on the project's verification standard.

---

# Phase 25P: Full Regression

Run:

```powershell
$env:PYTHONPATH="C:\Users\asus\Desktop\Hasni\BRE MIX DEZ\BRE-MIX-DESIGN"
python -m pytest tests/
```

Do not accept:

```text
"most tests passed"
```

The required condition is:

```text
0 failed
```

Record the exact count.

---

# Phase 25Q: Final Walkthrough

Update:

```text
08_AI_AGENT/PHASE_2/walkthrough.md
```

with:

```markdown
# Phase 25 - DATA-002 / Figure 5

## 1. Objective

## 2. BR 331 Source

## 3. Figure 5 Interpretation

## 4. Variables

## 5. Units

## 6. Curves Identified

## 7. Digitization Method

## 8. Digitized Dataset

## 9. Valid Domain

## 10. Interpolation Method

## 11. Extrapolation Policy

## 12. Example 1 Validation

## 13. EQ-BRE-004 Audit

## 14. Stage 1 → Stage 4 Integration

## 15. Figure 6 Dependency

## 16. Tests

## 17. Provenance

## 18. Engineering Limitations

## 19. Final Status

## 20. Next Dependency
```

---

# Decision Gate

At the end of this phase, you want the architecture to look like:

```text
DATA-004 Figure 3
        │
        ▼
EQ-BRE-001
        │
        ▼
DATA-001 Figure 4
        │
        ▼
LKP-BRE-001
        │
        ▼
EQ-BRE-003
        │
        ▼
DATA-002 Figure 5
        │
        ▼
EQ-BRE-004
        │
        ▼
       ┌─────────────────┐
       │ STAGES 1 → 4    │
       │ VERIFIED CHAIN  │
       └────────┬────────┘
                │
                ▼
       DATA-003 Figure 6
       PENDING USER DATA
                │
                ▼
          Stage 5 blocked
```

## Your immediate next instruction to the coding agent

You can give it this directly:

> **Begin Phase 25: DATA-002 / Figure 5. Freeze the current 76-test Stage 1–3 state first. Then perform a source-first audit of BR 331 Figure 5 and the existing EQ-BRE-004 implementation. Do not modify EQ-BRE-004 until the source audit is complete. Identify the exact Figure 5 variables, units, all plotted curves, curve labels, valid domain, interpolation method, rounding behavior, and Example 1 validation point. Digitize every required curve into `04_ENGINEERING_DATA/GRAPHS/FIGURE_05/figure_05_points.json` with full provenance metadata and digitization notes. Do not invent values, synthesize curves, or extrapolate outside the graph domain. Create dedicated Figure 5 data tests, including boundary and out-of-domain tests. Then implement/refactor EQ-BRE-004 using the verified DATA-002 provider, preserving full internal precision. Integrate and test the Stage 1 → Stage 4 chain against BR 331 Example 1. Keep DATA-003 / Figure 6 strictly blocked and do not bypass its provenance lock. Update the engineering registers only after the source data and implementation have independently passed verification. Finally run the complete pytest suite and produce a Phase 25 walkthrough.**

### After Phase 25

The roadmap should be:

**Figure 5 → EQ-BRE-004 → Stage 4 verification → Figure 6 / DATA-003 → EQ-BRE-005 → EQ-BRE-006 → Stage 5 → final rounding/presentation → complete BRE 331 mix-design validation.**

The big milestone you're approaching is a **fully traceable BR 331 calculation chain**, where every graph-derived number has a source, every equation has a test, and every unavailable engineering datum causes the engine to stop rather than quietly inventing an answer.
