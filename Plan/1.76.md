Yes. **Proceed to EQ-BRE-005 investigation and implementation.**

The DATA-003 decision is appropriate. One small terminology point: `[PENDING_USER_DATA]` is fine as a project status if your register supports it, but keep it clearly distinct from the established engineering statuses. The important part is that **Figure 6 cannot enter production calculations**.

Now the next step should be **source-first investigation of C5**, not implementation based merely on the agent's existing understanding.

Use this prompt:

Proceed to **EQ-BRE-005: Fine Aggregate Content (Calculation C5)**.

Follow the strict source-first engineering workflow.

### 1. Source-first investigation

Before changing implementation code, locate the exact BR 331 procedure for Calculation C5.

Document:

* BR 331 section/subsection
* Printed page
* PDF page
* Exact calculation identifier
* Exact wording surrounding C5
* Mathematical relationship
* Inputs
* Output
* Units
* Any assumptions or constraints
* Relationship to Stage 4 / EQ-BRE-004
* Relationship to Figure 6 / DATA-003
* How the selected Figure 6 proportion is converted into fine aggregate content

Do NOT infer the procedure from the existing code.

### 2. Establish the engineering specification

Create/update the appropriate engineering specification before implementation.

The specification should explicitly define something equivalent to:

**Fine aggregate content = Total aggregate content × proportion of fine aggregate**

But verify the exact BR 331 formulation, especially whether the proportion is represented as a percentage or decimal fraction and exactly how BR 331 applies it.

Document the conversion explicitly.

For example, if BR 331 gives a proportion of `p %`, determine whether implementation must perform:

`Fine aggregate = Total aggregate × p / 100`

Do not assume this without verifying the source.

### 3. Provenance separation

EQ-BRE-005 must accept the **Figure 6-derived proportion as an injected input**.

Do NOT make the equation function directly responsible for reading `figure_06_points.json`.

This keeps:

`Figure 6 data lookup`
→ `selected fine aggregate proportion`
→ `EQ-BRE-005`

as separate responsibilities.

This is important because DATA-003 is currently:

`PENDING_USER_DATA`

Therefore the equation itself can be mathematically verified independently of the unavailable Figure 6 dataset.

### 4. Figure 6 production guardrail

Create an integration test demonstrating:

1. Production requests the required Figure 6 value.
2. DATA-003 is unavailable.
3. The provider raises `EngineeringDataUnavailableError`.
4. EQ-BRE-005 is NOT executed with fabricated/default data.
5. The production pipeline does not silently substitute a value.

Do NOT use synthetic Figure 6 values as production inputs.

If synthetic values are useful for testing the equation's arithmetic, clearly label them as test fixtures only.

### 5. Engineering validation tests

Create `tests/equations/test_eq_bre_005.py`.

Test at minimum:

* Exact arithmetic using numerical values from BR 331 worked examples where available.
* Percentage-to-fraction handling.
* Normal valid inputs.
* Zero/negative total aggregate.
* Invalid proportion values.
* Boundary proportions.
* Unit consistency.
* Floating-point behaviour where relevant.

If BR 331 does not provide a numerical example specifically exercising C5, state that explicitly rather than inventing a "BRE example".

### 6. Integration

Create/update the Stage 5 pipeline test.

The intended production chain should resemble:

`Stage 4 Total Aggregate`
→ `DATA-003 Figure 6 lookup`
→ `Fine Aggregate Proportion`
→ `EQ-BRE-005`
→ `Fine Aggregate Content`

The Figure 6 lookup must remain provenance-controlled.

If DATA-003 is unavailable, the chain must stop cleanly.

### 7. Do not solve Figure 6 yet

Do NOT:

* generate Figure 6 coordinates
* create mathematical approximations of the curves
* populate empty JSON arrays with estimated values
* enable any synthetic-data bypass in production
* claim that EQ-BRE-005 makes Stage 5 operationally complete

The equation can be implemented and verified while its upstream engineering data remains blocked.

### 8. Update project documentation

Update:

* `EQUATION_VERIFICATION_MATRIX.md`
* `DATA_READINESS_REGISTER.md`
* `task.md`
* `walkthrough.md`
* relevant provenance/index documentation

Use:

`EQ-BRE-005 = IMPLEMENTED_PENDING_VERIFICATION`

unless the evidence genuinely supports a different status.

Clearly record:

`DATA-003 = PENDING_USER_DATA`

and its effect on production availability.

### 9. Regression testing

Run:

1. EQ-BRE-005 tests
2. Stage 5 integration tests
3. affected Stage 1-4 tests
4. complete `pytest` suite

Report the exact test count and result.

### Final report

Return:

1. Exact BR 331 source for C5
2. Exact mathematical formula
3. Input/output definitions and units
4. Whether a BR 331 worked example validates C5
5. Implementation files
6. Test files
7. Test results
8. DATA-003 dependency
9. Confirmation that production cannot bypass the Figure 6 provenance lock
10. Updated status of EQ-BRE-005
11. Next Stage 5/6 dependency

Important:

**Do not treat passing software tests as engineering-source verification.**

We are verifying two separate things:

* **Algorithm correctness:** does EQ-BRE-005 implement C5 correctly?
* **Engineering data correctness:** do we have verified Figure 6 values?

The first can proceed now. The second remains blocked.

### Where we are now

The architecture is becoming nicely modular:

```text
BR 331
  │
  ├── Figure 4 ──► W/C ratio
  │
  ├── External max W/C ──► final W/C
  │
  ├── C3 ──► Cement
  │
  ├── Figure 5 ──► Wet density ──► C4 ──► Total aggregate
  │
  └── Figure 6 ──► Fine aggregate proportion ──► C5 ──► Fine aggregate
                                                   │
                                                   ▼
                                            Coarse aggregate
```

The key thing now is **C5 can be verified without pretending Figure 6 is verified**.

Paste the agent's EQ-BRE-005 report after it finishes, and we'll audit that before moving on.
